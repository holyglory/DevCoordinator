# DevOps Board repository identity and layout incident

## Outcome

Status: diagnosed

The duplicated-project and repeated Project Load behavior is reproduced and traced to source-scoped project identity over three same-host inventories; the supplied signed-Board screenshot also preserves the unresolved top-cropped center panel. No product fix or SQLite cutover is claimed in this initial report.

## Cause

Class: implementation
Confidence: confirmed
Request: DevOps Board must show one project for one local repository, show Nevod once, keep non-repository resources out of the project tree, and keep the center panel fully visible below the window toolbar. The user also requested a fundamental account of how repositories, servers, Docker resources, and related state are stored and how that design should be corrected.
Immediate cause: The Board discovers the account coordinator plus two legacy Parall coordinator homes. Each source independently returns the same host-global Docker inventory. The Board deduplicates physical containers, but it keys `project_usage` and `ProjectGroup` by `(source origin, usage_key)`, so one canonical Git root becomes one project per source and the non-selected copies can become empty shells. The Project Load strip consumes those source-scoped rows directly, producing three Nevod entries. The coordinator also emits `name:*` rows with no project path for unattributed containers, and the Board presents each row as a project. The screenshot separately confirms that restored window geometry allows the center panel's top content to render above the visible content boundary; the exact layout correction remains part of the pending product repair.
Why missed: The multi-source regression fixture asserts source separation at the project level to avoid native server-ID collisions instead of modeling one repository with source-scoped resource references. This contradicts the recorded 2026-07-07 repository-grouping decision. The previous narrow-pane check covered horizontal width but did not exercise the real restored-window top-boundary failure shown by the user. Launch verification proved source and server counts, not canonical repository cardinality, unassigned-resource behavior, or top-edge visibility.
Evidence: Direct real inventory on 2026-07-13 returned source contributions of 16/3/0 presented servers, while every source returned the same 15 Docker containers and seven PostgreSQL projections. Their 11/9/9 project-usage rows total 29 rows for 13 distinct usage keys; only ten are path-backed, nine are currently valid Git roots, and one is a missing historical path. All three sources returned the same redacted Nevod path identity with two containers and approximately 3.73 GB. All three returned `name:aicursegmailcheck` with no project path. Persisted stores were 8,211,325, 1,550,427, and 496,675 bytes; the current two-pass inventory path rewrites approximately 39.13 MiB of JSON payload per Board refresh and observes the same Docker engine once per source. The 2346x1698 user screenshot has SHA-256 `ab7bf1bbb8ba49c53beecd61ecc043a42339ed355400483262f05f06794b9e8e` and visibly shows the toolbar/top of the center panel outside the content boundary, repeated left-tree projects, and three Nevod load rows. The detailed source trace and proposed target are recorded in [`repository-catalog-and-state-store.md`](../architecture/repository-catalog-and-state-store.md).

## Changes

Product: This incident artifact and architecture investigation are read-only with respect to product code and service state. The root task owns the pending Board layout and repository-presentation repair; this report does not state that those changes, the normalized SQLite store, or legacy-state import are implemented.
Prevention: A normalized repository catalog, source/host identity separation, read-only inventory contract, one-observation-per-Docker-engine model, transactional same-UID legacy import, and realistic repository/layout regression matrix are proposed in the architecture document. They are not applied by this documentation-only step.

## Verification

Original path: The pre-fix original path was exercised through the same real coordinator inventory inputs used by the signed Board and compared with the supplied Board screenshot. It reproduced three source contributions for one Nevod Git root, repeated name-only pseudo-project rows, and physical Docker observations repeated across all sources. The screenshot verifies the outstanding center-panel top crop. No post-fix original-surface result is claimed while status remains diagnosed.
Checks: The investigation inspected the live private state structure and aggregate counts without exposing full private paths, traced `canonical_project`, `build_project_usage`, Docker observation, Board source discovery, `mergeProjectUsage`, `makeProjectGroups`, action routing, and the existing multi-source regression fixture. It confirmed raw duplicate logical server records, exact duplicated legacy assignments, stale cross-project port conflicts, and retired Docker telemetry series. The formal report verifier passes this diagnosed report; product, migration, native layout, sustained refresh, and signed-package checks remain for the root task after implementation.
Residual risk: The current product still has the reported repository duplication and top-crop behavior until the root task completes and retests it. Simply removing the source from the UI grouping key would make current project actions ambiguous and could repeat a host-global Docker action, so repository identity, resource provenance, aggregation, and control routing must remain consistent. The proposed SQLite architecture and cross-user broker are not implemented; migration conflicts and the exact native layout correction still require verified product work.
