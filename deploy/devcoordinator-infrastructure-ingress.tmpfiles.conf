# Configuration remains root-owned and group-readable only by the dedicated
# ingress account. The broker evidence CAS is never writable by ingress.
d /etc/devcoordinator/infrastructure-ingress 0750 root devcoord-infra-ingress -
d /var/lib/devcoordinator/infrastructure-envelope-artifacts 0700 root root -
