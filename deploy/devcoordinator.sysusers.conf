# The locked account gives the test scheduler its own cgroup/process boundary.
# Its UID is execution attribution only and is not a local IPC authorization
# credential.
u devcoordinator-testd - "DevCoordinator test scheduler" /nonexistent /usr/sbin/nologin
