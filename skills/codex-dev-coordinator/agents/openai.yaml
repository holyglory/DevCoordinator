interface:
  display_name: "Codex Dev Coordinator"
  short_description: "Coordinate attributed local runtimes"
  default_prompt: "Use $codex-dev-coordinator to inspect or change this repository's runtime through the attributed host authority."
