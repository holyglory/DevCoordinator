# User Issue Ledger: Operations

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-OPERATIONS-016 | Repository-scoped continuation commands | A runtime or test mutation returned a valid continuation handle but omitted the repository selector, so following it from another working directory contacted the wrong repository and falsely reported the operation unavailable. | Every generated operation or test continuation command carries the resolved canonical `--project` scope whenever repository context was established; a pre-context failure preserves an explicit caller-supplied project when available. The command remains shell-safe and executable from any working directory. | Produce pending runtime, operation-follow and test continuations for a repository path containing spaces, invoke each from a different working directory, and prove it resolves the same repository and exact durable identity. Preserve the compact unscoped form only when no repository context is known. |
| UIL-OPERATIONS-001 | Coordinator calls and failure diagnosis | A client could report a Coordinator admission or trust-path failure after the responsible authority, test-plane, or snapshot call and detailed cause had disappeared from routine evidence, forcing reconstruction from another agent's prose. A completed host action later wrapped a typed post-action observation mismatch as generic uncertainty, so the call journal retained no actionable inner stage or code. | Every actual Coordinator call, including malformed, rejected, failed and successful calls, emits bounded structured received and terminal records at the relevant local RPC boundary with time, duration, caller attribution, native operation/request correlation, non-secret resource identifiers and a sanitized outcome. Internal test and snapshot calls preserve the typed stage, subject, exception class and errno before public error flattening. A completed host action distinguishes a committed observation that proves the requested state was not reached from an observation that failed to commit; the former returns its exact typed mismatch, while the latter retains a bounded path-free inner stage/code in the operation fence and rolling log. The shared JSONL log uses a multiprocess lock, rotates under a fixed byte and file-count ceiling, survives service replacement, is readable and writable by the trusted same-developer services, and never records raw request/result payloads, paths, credentials, environment values or secret-bearing arguments. A caller-side filesystem probe that never contacts Coordinator is not reported as a Coordinator failure; the absence of a correlated call is itself diagnostic evidence. | Exercise success, typed rejection, unexpected failure, malformed payload, capacity rejection, disconnected reply, snapshot dependency failure, concurrent processes and service replacement. After a successful Compose/Docker invocation, separately inject a committed stopped-state mismatch and an observer commit failure; prove the first terminalizes with the exact mismatch code and the second remains fenced with its bounded inner observation code. Prove paired operation/request correlation and useful path-free root-cause identity, explicit redaction, valid one-record-per-line JSON, deterministic rotation with no more than the configured files/bytes, and a production diagnostic read that finds freshly issued known calls at authority, test-plane and snapshot boundaries. |
| UIL-OPERATIONS-002 | Headless browser lifecycle | Agent and test browser process trees could outlive their caller, consume tens of GiB indefinitely, remain outside repository accounting, and require manual discovery because the Coordinator neither observed their activity nor owned idle cleanup. | Coordinator identifies only explicit headless/automation browser trees, measures their CPU, working memory and last observed work, attributes managed sessions when available, and reaps abandoned eligible trees after a bounded idle interval. Project-contained browsers stay in their project accounting, governed test browsers remain owned by their runner, PID reuse cannot target another process, and a release that first introduces this lifecycle performs one bounded all-headless cleanup immediately before activation. | Exercise activity and inactivity transitions, process churn, PID reuse, protected project/test cgroups, TERM-to-KILL escalation, respawn, bounded retained cleanup evidence, and release replay. Before production activation, prove current headless automation trees are removed; afterwards prove a managed canary appears in Performance, activity refreshes its timestamp, idle expiry removes its exact tree, and interactive browsers are never selected. |
| UIL-OPERATIONS-003 | First-use repositories and development servers | A valid newly initialized repository could not use the installed Coordinator because the static client rejected its root before contacting the healthy broker, and one production acceptance path later tried to traverse `.git` as the client account and failed with `EACCES` before any broker call. After authority adoption succeeded, the Console's long-lived protected client still used only its stale installed profile for a read-only log request, rejected the newly adopted repository locally, and raised a global 409 banner. Its later dynamic resolver still looked only for a target enrollment owned by the protected API account, so a repository correctly adopted by another trusted local account remained invisible. The stable test CLI repeated that defect by resolving an explicitly selected repository only from its static installed profile, even though the same authority-adopted repository was usable through the intent client. Other first-use failures were flattened into broker-outage or generic inspect-logs advice, filesystem stat owner was incorrectly used to choose the execution account, acceptance reused the application's ordinary port, and a nested application directory was mistaken for the Git root. Later adoptions succeeded in authority storage but their clients rejected the success reply because producer-only UID evidence—and then a retired image-prefetch allowlist field—had never been round-tripped through the strict repository parser. Production then used the right caller UID but only one compatibility GID even though the repository root and nested application directory used different groups; after adding both groups, systemd's own pre-exec `WorkingDirectory=` step still failed before the structured command could run, while `--quiet`, pre-catalog diagnostic loss, and a transport/broker-outage classification initially hid the definite launch rejection. The final launch then reached the actual caller but a third account's generated `0700` executable had collapsed the inherited POSIX-ACL mask to `---`, so the named caller entry existed but was ineffective. | Read-only discovery remains non-mutating and reports a valid unknown repository as unenrolled without client-side Git traversal or a false broker-outage claim. A protected non-executing reader proves repository identity. Every read-only control-plane action—including stable test catalog and planning—dynamically resolves an already authority-adopted repository through an existing current transport anchor, so a stale installed profile never blocks status, logs, or test setup; when the protected caller has no target-specific enrollment, the resolver reuses an enabled, current routing policy belonging to another trusted local account without changing the physical caller UID. Absence of a per-account row is not a local authorization denial. Enabled policy UIDs within one account form a union; an account-wide block disables every matching enrollment or uses the exact repository/action/lifecycle revocation. This refresh cannot perform first-use adoption. The first typed start-like request atomically adopts the Git root, creates or refreshes routing/execution state for the actual non-root local caller, durably records that UID as both attribution and execution identity, and runs the structured no-shell command only as that caller. UID/GID/owner/mode/ACL metadata is diagnostic and routing/execution compatibility evidence, never local authorization; filesystem stat owner, root, and control-plane identities never substitute for the caller UID. Repository-profile producers emit exactly the strict parser's current field set; retired authorization/policy fields are never serialized. The transient unit keeps the caller's primary GID and may add existing non-root filesystem GIDs along the resolved working-directory chain solely as per-unit supplementary compatibility groups, without changing account membership; group 0 is never added. For a fresh launch, exact-port preflight precedes a descriptor-anchored walk of only the validated cwd. The walk prunes `.git`, never follows symlinks, silently skips multiply linked regular files, and adds only group-class `rwX`/ACL-mask bits while preserving bytes, UID/GID, named ACL entries, world bits, and regular-file executable intent. A port collision performs no metadata mutation; there is no `UMask=0000`, broad source-root normalization, or world-access grant. Systemd starts one fixed Coordinator-owned credential-drop shim in `/`; the shim applies the caller identity before a trusted `env --chdir` boundary enters the validated repository cwd and execs the structured argv without a shell or elevated capability. The Coordinator proves all four Linux UID fields of the stable ready MainPID equal that caller and stops the unit if it cannot. Every failure identifies stage, broker contact, mutation, typed cause, retryability, and one exact next action, including bounded native stderr or journal evidence when systemd rejects a unit before it enters the runtime catalog. A definite systemd rejection is classified as a launch infrastructure failure even if secondary diagnostic collection fails. Local fallback stays disabled. Production acceptance uses a run-unique service and dedicated ephemeral test port, while canonical Git root and contained application cwd remain separate inputs. | In the software-owned delivery cycle, use a zero-commit repository with untracked source, an installed-but-stale profile, and a non-root caller whose UID differs from filesystem stat owner and has no prior routing record. Prove discovery makes no mutation and needs no client-side `.git` traversal, then perform one idempotent first-use start, fetch the strict-port server, and clean it up. Before replacing the installed profile, use the protected Console and stable test clients with no target enrollment to resolve that authority-adopted repository through another enabled local routing policy, capture exact-resource logs and read test catalog/plan state without mutation or a global error banner, and prove the physical caller remains the attributed peer. Then disable every matching enrollment for that route account and disable the exact runtime action; prove neither case falls through to another account's enabled policy. Round-trip the exact live `repository.ensure` reply—including paired attribution-only execution/filesystem-owner evidence—and assert exact equality with the strict parser's allowed field set before launch. Assert the command executes with the actual caller UID, keeps the caller's primary GID, adds every non-root existing filesystem GID needed across a mixed-GID nested cwd only as per-unit supplementary source compatibility, performs the validated cwd transition only after credentials are applied, retains the caller UID in operation evidence, proves the ready MainPID's real/effective/saved/filesystem UIDs, stops on any mismatch, and performs no group-0 grant, shell execution, chown, account group enrollment, named-ACL-entry edit, owner-derived UID selection, root execution, capability grant, control-plane execution, local fallback, or manual enrollment. Seed a third-account `0700` executable plus a regular non-executable, `.git` metadata, an external symlink, and a multiply linked file; prove only eligible cwd group-class mask/access bits change, executable intent and world bits remain stable, `.git` and external/multiply linked inodes remain unchanged, and no `UMask=0000` is emitted. Cover a systemd pre-catalog rejection and prove the persisted operation, installed client envelope, and rolling call journal retain a bounded sanitized native diagnostic and a `launch`/`infrastructure_failure` envelope even when journal lookup itself fails. Cover concurrent first use, lost-reply replay, linked worktrees, nested application cwd, path escape, explicit disabled action/lifecycle, port collision without hopping or normalization, broker unavailability, TTL cleanup, call-journal correlation, and normal Python/`python -O`. Require bounded stage/contact/mutation/retry/next-action evidence for expected and unexpected failures, and prove public identity and secret boundaries remain unchanged. |
| UIL-OPERATIONS-017 | Repository Compose resealing after an uncertain operation | A prior Compose action remained in `needs_attention`, then a repository manifest changed its Compose definition. Enrollment refused to reseal the definition while that operation was unresolved, but the normal runtime request that would reconcile the operation could not pass enrollment, permanently deadlocking first-use and delivery acceptance. | During an authorized repository ensure, the authority finds an exact prior `needs_attention` operation for the repository's still-sealed Compose definition, reconciles it once from a fresh complete Docker observation, and only then seals the current manifest definition. Running, partial, foreign, ambiguous, or unobservable operations remain fenced. Any remaining definition-change failure names the exact blocking operation and status. | Seed an uncertain operation against definition A, change the repository manifest to definition B, and prove one repository ensure observes and terminalizes A before B is persisted. Reject wrong repository/definition identity, incomplete observation, multiple non-reconcilable states and races. The full first-use acceptance must then launch, fetch, collide safely, follow from a fresh process, and prove TTL cleanup without manual database or enrollment edits. |
| UIL-OPERATIONS-018 | Consecutive software-owned deployments | A complete delivery packaged a new immutable release but reused the previous release's same-schema transaction directory, so preparation correctly rejected the foreign journal and blocked every subsequent deployment before mutation. | The CLI's transaction-root argument is a durable parent. Every packaged release derives one exact digest-named child transaction directory and uses that same child for prepare, apply, health, rollback, clean adoption, replay and reporting. A prior release journal can never collide with a later release, while replay of the same release remains deterministic. | Complete two successive deliveries with distinct immutable release digests under one configured transaction parent. Prove each command receives its own digest child, the second never reads the first journal, all phases of one release share the same child, and replay of either release remains bound to its original journal. |
| UIL-OPERATIONS-019 | Caller deadlines across first-use adoption and launch | A caller selected a development-server launch deadline, but the client used an unrelated hidden 10-second transport timeout for the repository-adoption mutation that precedes launch. The authority completed adoption seconds later, while the command and delivery acceptance had already reported an uncertain failure. | One start-like command propagates the caller's selected launch budget to every nested adoption and launch transport, retaining only a bounded response margin. Repository ensure also has a safe mutation default longer than the generic read timeout for callers without a semantic override. A transport slice never shortens the caller's requested deadline, and a genuinely lost reply retains the exact operation continuation. | Delay repository adoption beyond 10 seconds but within the caller-selected launch budget and prove the same command proceeds to one launch with no duplicate adoption. Assert the exact transport override reaches repository ensure, default ensure timeout is mutation-appropriate, deadline exhaustion returns one scoped continuation, and replay/follow converges on the single durable operation. Complete the installed first-use acceptance. |
| UIL-OPERATIONS-020 | Exact runtime log capture | The authority captured and retained a bounded exact-resource log artifact, but the stable agent CLI projected the successful response as `resource: null` and discarded both the artifact descriptor and its content. Operators and agents could not diagnose the failure without bypassing Coordinator. | A successful `runtime capture_logs` response returns the exact artifact descriptor and a sanitized bounded tail of its content through the stable CLI, preserving multiline diagnostics and explicitly marking projection truncation. Artifact and content identities must match; status and mutation responses remain compact. | Capture service and Docker logs containing long output, control characters and multibyte text. Prove the CLI result stays within 8 KiB, preserves the newest diagnostic lines, marks truncation, binds matching artifact IDs, rejects contradictory producer output, and exposes no private host path. Reproduce an installed failed runtime and diagnose it using only the stable Coordinator surface. |
| UIL-OPERATIONS-021 | Governed test plan submission and continuation | A protected client dynamically resolved and registered an adopted repository test plan, then fresh submit and follow processes refused that exact plan or run when the repository had either no static local row or multiple rows. The authority-owned identities were already sufficient, so the extra client-side enrollment gate falsely instructed agents to reinstall Coordinator. | Submission, status, summary, failures, artifact, cancellation, retry and wait accept the canonical root used for planning, dynamically resolve its current authority enrollment, and send the resulting immutable repository ID plus exact plan/run identity to the test plane. An explicit immutable ID remains supported when a current route is already present. The test plane proves ownership and authorizes the peer; absent, duplicate or stale static rows cannot block or redirect an exact registered plan or run. | Register and submit a plan through a dynamically resolved repository, discard that process, then submit and follow the run by canonical root from fresh static profiles with zero target rows. Repeat with duplicate rows sharing its repository ID. Prove every read/operate command sends the exact repository and plan/run pair, rejects cross-repository identities at the test-plane boundary, and preserves idempotent mutation replay. |
| UIL-OPERATIONS-004 | Software-owned release replacement | A fully verified candidate was blocked before mutation because the retiring release contained interpreter-generated `__pycache__` files from a legacy launcher; a strict candidate-integrity verifier was incorrectly reused as a permission and cleanliness gate on the already-running predecessor. | Candidate releases remain strictly content-addressed and fully verified. Replacement derives the retiring release's one required transition capability from its digest-bound manifest inventory without checking local ownership, modes, or generated bytecode, and direct worker Python launchers disable bytecode writes. Generated cache residue in a predecessor never blocks a healthy candidate switch. | Package a strict clean candidate, add non-canonical bytecode with restrictive modes to the retiring fixture, prove capability planning and switch preparation proceed, and prove a malformed or digest-contradictory retiring manifest still fails. Run the complete software-owned delivery cycle and verify the live switch and post-switch health. |
| UIL-OPERATIONS-005 | Actual-caller working-tree preparation | The fixed exact-cwd normalizer was correct in source, but the first authority sandbox omitted a writable `/home` view and returned `EPERM`. The next fix added `ReadWritePaths=/home` while retaining `ProtectHome=read-only`; that separate mount still won and returned `EROFS`. The client initially mislabeled the definite launch-stage rejection as `broker_unavailable` and gave no host repair. | The root authority receives a writable `/home` sandbox view solely for fixed code-bounded exact-working-tree normalization across all trusted local accounts: `ProtectHome=false` makes the explicit `/home` exception under `ProtectSystem=strict` effective. Repository code never inherits root or that view. A missing or ineffective view, or `EACCES`/`EPERM`/`EROFS`, is a certain launch `infrastructure_failure` with path-free errno and exact authority-boundary recovery, never a broker outage or project defect. | The availability topology rejects either an authority unit without `/home` in `ReadWritePaths` or any authority `ProtectHome` value other than `false`. Unit tests inject `EPERM` and require typed actionable evidence. The complete live first-use acceptance must normalize a third-account `0700` Vite target, launch as the actual caller, fetch it, prove the caller UID, and clean it up. |
| UIL-OPERATIONS-006 | Out-of-band Coordinator bug intake | Coordinator failures were reported only through chat or the normal authority path, so the reporting channel could fail with the component being reported, duplicate reports lacked stable correlation, closed defects remained as synchronization noise, and reports could not be transferred safely between independent servers. | A bounded server-wide open-bug registry is independent of authority, API, broker, testd and repository enrollment. Trusted local accounts can atomically report/list/close structured defects without local ownership authorization. Presence in the canonical open directory means open; closing removes the exact file, with no closed row or tombstone. Identical local active reports deduplicate while retaining first/last seen and occurrence count. Records carry portable redacted reproduction steps, structured commands, repository name, correlations, and stable local-or-remote origin identity, but no secrets, private host paths, repository-presence assumption, or unbounded raw logs. Imported records never deduplicate into a local observation merely because their failure fingerprint matches. | With all Coordinator services/profile discovery disabled, exercise CLI, dedicated launcher and MCP report/list/close; prove concurrent same-fingerprint local reports converge, remote-origin imports remain distinct and idempotent across re-export, concurrent readers never observe partial JSON, exact close removes the physical record for every Console instance, malformed files are isolated, records/results stay bounded, and obvious credentials/private paths are redacted. Verify tmpfiles and Console sandbox wiring against the confirmed same-developer local-trust assumptions. |
| UIL-OPERATIONS-007 | Console control-plane availability | The public edge and Console shell remained healthy while the required loopback API socket and every Console backend slot were inactive, so authenticated pages returned 502. The Console template had crash restart policy but no boot-install target, and routine same-schema replacement assumed one slot was already active before it could repair anything. It also verified only current active state, allowing a disabled required unit to look recovered temporarily and disappear after reboot. A later post-promotion health failure attempted to promote the previous slot without transferring the candidate's live writer lease, so automatic rollback stopped incomplete. The first repair merely disabled the superseded broker; an enabled project unit retained reverse `Wants=` dependencies and `Restart=always`, so each project restart revived both obsolete control-plane services and invalidated the otherwise healthy cutover. | A zero-slot switch recovers only the exact immutable slot retained by the atomically published edge snapshot, starts the stable API socket, and never guesses from installed units; two active slots remain an error. Every promoted Console instance and required stable socket/service is both boot-enabled and active. The API socket owns the stable listener, API and Console processes use crash and clean-exit restart supervision, and the edge stays available during repair. Superseded broker/API units have a durable systemd retirement guard as well as being stopped and disabled, so stale reverse dependencies cannot reactivate them during a switch, rollback, project restart, or reboot. Readiness requires active and enabled evidence plus live loopback API and public-edge probes. When health fails after promotion, rollback transfers the writer lease from the candidate to the previous slot before restoring publication, then drains the candidate. | Test zero-slot recovery against the exact publication/slot pair and reject mismatched or ambiguous topologies. Start from an inactive API socket and disabled-but-manually-started required units. Model an enabled `Restart=always` project service that wants both legacy units and prove the retirement guard keeps both inactive and disabled while the current authority/API remain enabled. Inject a post-promotion health failure and prove rollback promotes the previous slot with the candidate control socket as its old-writer handoff, restores public health, disables the candidate, and retains legacy retirement. Run the complete software-owned delivery cycle and prove all required sockets/services plus the promoted Console are enabled and active, both superseded units are guarded/inactive/disabled, public edge availability is preserved, and both state classes are reported. Verify live loopback/authenticated Console recovery and supervision; systemd unit verification plus enabled-state evidence must prove reboot activation. |
| UIL-OPERATIONS-008 | Shared local browser metadata | Production browser acceptance and read-only headless-browser accounting ran as the actual developer account but failed before any browser check because the non-secret runtime inventory and lifecycle observation lived behind a root-private authority database directory, and the read-only lifecycle reader also opened its lock with write access. Root-run deployment had hidden both defects. A retired broker and the canonical first-use tmpfiles policy could independently restore that shared directory to its historical private mode after a repair, so mode repair inside the database parent was inherently unstable. | Authentication storage and signing keys remain private. Publish only the sealed, non-secret browser runtime inventory through a root-controlled world-readable projection. Store root-produced browser lifecycle state and its coordination lock in a dedicated caller-readable state root that no authority database service or legacy tmpfiles policy owns; publish both files as read-only telemetry and acquire the existing lock read-only for status calls. Replacement installs and verifies every canonical tmpfiles policy and durably retires superseded services. No local account ownership or write permission is required for either read journey. | Run browser acceptance, first-use adoption, and accounting in that order as an ordinary configured developer account after deployment. Assert the public runtime projection is byte-identical to the retained private source and mode 0644, the dedicated lifecycle root is mode 0755, lifecycle state/lock are mode 0644 and readable without write permission, authority databases remain private, both installed tmpfiles policies match the candidate release, every superseded broker is inactive and disabled, and the complete production Playwright journey succeeds. |
| UIL-OPERATIONS-009 | Trusted-local runtime lifecycle and declared first use | An exact enrolled runtime target resolved successfully but was denied because the selected installed profile account lacked a per-UID start row; another enrolled repository declared a database runtime in its canonical manifest but had no sanctioned way to materialize its first target. Both are local-account permission/enrollment artifacts on the confirmed single-developer host, not meaningful authorization boundaries. Runtime grants were later unioned across trusted local accounts, but a newly observed declared container still had no row at all and was denied before its first lifecycle action. The equivalent Compose and run-once grant paths also required the physical policy UID's row, so an authority-adopted declared stack remained denied when another local account held the exact grant. The first adoption implementation then existed only in fixture persistence: the production broker constructed persistence without its service-owned Compose renderer, so installed first use failed before enrollment despite every source suite passing. After that wiring was fixed, the installed journey still classified an explicitly loopback-only published port as host-equivalent public access and demanded another administrator approval; the first classifier recognized structured `host_ip` evidence but not the short syntax retained by `compose config --no-interpolate`. | Any non-root local caller may perform an exact supported lifecycle, Compose, or governed run-once action on an active, enabled, current-generation enrolled resource regardless of which trusted local account originally populated its exact ACL row. Exact active repository membership is the trusted-local default when no resource/action ACL row exists; an explicit disabled row still denies the action. All enabled local principals and enabled exact repository enrollments form one grant union; physical caller UID remains attribution. Explicit repository/resource/action disablement, stale generation, immutable target identity, idempotency, source containment, TTL, and lifecycle fences remain. A sealed canonical repository runtime declaration can be validated and atomically bootstrapped into one attributed exact target on first `ensure ready`; its newly observed exact container is immediately usable through status/log/start/stop/restart without administrator enrollment. Arbitrary undeclared commands, images, mounts, or secrets remain impossible. The production broker and every restart/reopen path must wire the same bounded service-owned merged-model renderer used by enrollment and tests. Numeric loopback-only port publication is ordinary local development reachability and needs no case approval in either structured or retained short syntax; omitted, wildcard, malformed, or non-loopback host addresses and other host-equivalent features remain approval-required and the error names the triggering features. | Reproduce exact Docker, Compose, and run-once targets from a different local account and require the enabled exact grant from any trusted local enrollment to reach the host action while preserving the physical caller only as attribution. For a newly observed exact repository member, omit every runtime ACL row and prove status/log/start/stop/restart are allowed; insert an explicit disabled row or disable the repository enrollment and prove it rejects before host effects. Build the real store-backed broker constructor and assert its persistence can render a declared Compose model. From an enrolled repository with no targets, bootstrap one declared PostgreSQL stack with IPv4/IPv6 loopback publication through the installed client, including `compose config --no-interpolate` short syntax, prove idempotent replay returns the same immutable ID, status/log/stop/start work, and malformed, undeclared, stale-generation, secret-bearing, path-escaping, wildcard/public-port, or host-equivalent declarations fail before mutation. The software-owned deployment blocks on that installed first-use journey. |
| UIL-OPERATIONS-010 | Runtime CLI and exact cleanup resolution | Capabilities advertised runtime replacement while flag mode rejected the action, and cleanup planning decided target ownership from a bounded installed client profile whose container list was incomplete even though authority inventory held the exact membership. | Every advertised ordinary runtime action is callable through the structured flag adapter with all required no-shell fields and actionable validation. Cleanup planning resolves the supplied immutable target ID dynamically through the broker/authority inventory, requires exactly one current repository membership, and never depends on a stale or truncated client profile resource list. | Compare advertised actions to parser actions, round-trip `replace` with repeated argv fields, and require incomplete replacement input to name the missing field. Plan cleanup for an exact authority-enrolled container absent from the installed profile, reject zero/multiple/stale matches, and verify no mutation occurs during resolution. |
| UIL-OPERATIONS-011 | Project-scoped Docker storage accounting | Agents could see exact runtime identities but not physical Docker bytes or safe reclaim candidates, forcing either blind cleanup or direct Docker inspection outside Coordinator. | Coordinator's retained inventory attributes measurable container writable layers/logs and named-volume bytes to exact repository/resource IDs, reports shared image and build-cache bytes separately with truthful shared/unattributed ownership, and exposes bounded read-only storage summaries through capabilities/targets. Cleanup planning accepts only exact obsolete, unreferenced resource IDs and estimates reclaim without treating shared bytes as exclusive. | Feed shared images, referenced/unreferenced layers, writable layers, logs, and named volumes across two repositories. Prove exclusive and shared bytes are not double-counted, unknown sizes remain unknown, exact IDs and sample time are retained, and cleanup estimates never claim shared bytes as guaranteed reclaim. Reproduce GlobalFinance capabilities/targets with non-empty attributed storage and plan an exact stopped unreferenced cleanup without direct Docker access. |
| UIL-OPERATIONS-012 | Retained observer startup and refresh | The bounded observer ran a full-file SQLite `PRAGMA quick_check` before every refresh and in systemd `ExecStartPre`; once the retained store reached 176 MB, the scan exceeded the 90-second start deadline and blocked both release activation and rollback even though the publication and structural digest contract were valid. | Routine startup and refresh reconcile the exact published generation, metadata, payload digest, retention count and byte bounds without a full-file integrity scan. Full SQLite integrity remains an explicit offline diagnostic, never a service-start or polling prerequisite. | Build a large retained store or replace the integrity call with a blocking sentinel; prove config/start and refresh do not invoke it, remain bounded, and still reject malformed publication, digest, active-pointer, retention and schema contradictions. Run the explicit full-integrity command separately and prove it detects corruption. Complete a live switch with the existing large store inside the systemd deadline. |
| UIL-OPERATIONS-013 | Software-owned delivery single-flight | A lost tool-session handle was mistaken for a dead deployment controller; a second controller reused the same run root while the first later entered rollback, producing colliding step numbers and overlapping apply/rollback work. | Exactly one source-check/package/deploy/acceptance controller may own a run root at a time. A second invocation fails before reading or mutating durable state and tells the caller to wait for the first report; read-only report reconstruction remains available concurrently. | Hold one run-root lock, attempt every mutating delivery action from a second process, and prove each rejects without creating steps or invoking package/deploy commands. Release the lock and prove deterministic continuation works. Simulate a lost terminal attachment while the first controller continues and prove polling/reporting cannot launch a successor. |
| UIL-OPERATIONS-014 | Same-schema release rollback | Rollback restored installed files before restarting services, but used the forward fail-fast service loop. A timeout starting one background observer could terminate rollback before the authority and stable client were returned to the same release, leaving public health superficially available while agent calls failed the release handshake. | Rollback restores the authority socket and restarts the stable authority/API control path immediately after the previous files are restored, before any observer, notification, test, or other background unit can block. The remaining units are restored only after that coherent checkpoint; interruption or replay at any later point preserves or deterministically recovers the previous control plane. | Inject a timeout/failure for every background unit during rollback and prove command ordering has already restarted the previous authority and API, the stable client handshake is coherent, the public Console remains on the previous slot, and replay can complete all remaining units without reapplying host mutation. |
| UIL-OPERATIONS-015 | Developer-directed Docker cleanup | Coordinator treated an agent's clear obsolete-container decision as a storage-reclamation proof problem, rejected mounted Compose one-offs, forced archive before purge, then required an administrator-provisioned per-resource grant. The agent initially repeated those mechanical denials as if they contradicted the semantic decision, and nine obsolete GlobalFinance containers remained present on the confirmed single-developer server. | For a Docker container, the agent or user selects the target and owns the deletion decision. The stable client performs one attributed direct removal without repository ownership, cleanup ACL, archive, lifecycle-state, mount, Compose-role, database-binding, fingerprint, plan, confirmation, or revalidation gates. Python resolves the selected current Coordinator target to its full native container ID and invokes only `docker rm -f <64-char-id>` without `-v`; the result reports removed or already absent. Named-volume deletion remains a separate explicit data-retention decision and retains its exact plan/apply workflow. Coordinator never downgrades a clear semantic container decision into storage eligibility. | Through the installed client, remove stopped, running, mounted, standalone, Compose one-off, ordinary Compose, database-bound, cross-repository, and ACL-free container fixtures in one call and prove every request reaches only `docker rm -f <64-char-id>` with no `-v`, prune, Compose teardown, archive, grant lookup, plan, confirmation, or second observation. Prove an already-absent target is a truthful success, malformed or unresolved selectors do not run Docker, and every named-volume path still requires its existing plan/apply decision. Reproduce removal of the nine obsolete GlobalFinance initializer containers without administrator enrollment. |
| UIL-OPERATIONS-022 | Exact project-attributed systemd unit lifecycle | A project-owned maintenance unit could not be inspected or controlled through the stable runtime surface because non-worker services were rejected with `runtime_supervisor_required`, leaving direct `systemctl` as the only apparent path. | Coordinator admits systemd lifecycle only for an exact current project resource whose authoritative control binding and immutable native unit identity are already sealed. Status is observation-only. Start, stop and restart use the fixed native unit identity, persist one operation, reobserve the terminal state, reject aliases, foreign/stale/ambiguous bindings and unknown unit state, and never accept caller-authored commands or unit paths. | Exercise status/start/stop/restart and replay for an exact sealed project systemd unit, including enabled and disabled startup policy states. Reject display-name-only, mutable alias, wrong project, stale generation/fingerprint, non-systemd supervisor, missing/duplicate controller, unobservable state, changed unit identity, lost reply and PID/unit replacement. Reproduce the installed project maintenance journey without direct `systemctl`. |
| UIL-OPERATIONS-023 | Safe stale Codex state reconciliation | Codex retained state rows whose rollout files were missing, while its exposed deletion operation cascaded to descendants and could not prove an exact stale-row-only effect; direct SQLite editing or raw rollout deletion would bypass ownership and recovery controls. | Any reconciliation surface first inventories one exact stale state identity, proves its rollout is absent, reports every dependent row or task that would be affected, and emits a reviewable no-mutation plan. Apply requires an exact plan fingerprint and confirmation, revalidates the state graph, changes only the planned stale identity set, preserves live descendants and rollout files, retains bounded evidence, and is replay-safe. If the installed Codex version cannot provide those primitives, Coordinator reports the capability as unavailable and performs no mutation. | Model missing-rollout rows with no descendants, live descendants, shared ancestry, concurrent row/file changes, recreated rollouts, wrong account and stale plans. Prove preview is read-only, unsafe cascades are blocked, exact safe reconciliation is atomic and replayable, and no direct database or filesystem deletion occurs outside the owning product's supported authority. |
| UIL-OPERATIONS-024 | Trusted-local broker architecture | DevCoordinator kept repository-scoped grants, resource-membership authorization, controller/source authority, per-account enrollment allowlists, and group policy after the deployment had been confirmed as one trusted developer. Those layers blocked valid cross-repository commands and turned ordinary lifecycle work into administrator provisioning. | Every trusted local agent may issue every supported command for every current resource on the server. Repository association is optional presentation/routing metadata, never an access decision. Local broker requests have no repository, resource, action, account, UID, GID, group, membership, enrollment, controller/source, or per-resource permission lookup. Public identity, secret transport, payload validation, immutable target identity, stale-generation protection, path containment, idempotency, lifecycle correctness, execution attribution, and explicit destructive-data confirmation remain separate controls. | The authority schema contains no local ACL/grant, repository-membership, controller-binding, or source-authority tables. Static checks reject authorization joins and grant/deny branches on local command paths. From agents launched in two different repositories and Unix accounts, exercise status/start/stop/restart/log/test/lifecycle operations against resources associated with both repositories and prove identical target-based behavior. Explicitly prove malformed payloads, stale immutable identities, path escapes, replay conflicts, invalid lifecycle transitions, public Console access failures, and secret-policy violations still fail for their own reasons. |
