# User Issue Ledger: Operations

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-OPERATIONS-001 | Coordinator calls and failure diagnosis | A client could report a Coordinator admission or trust-path failure after the responsible authority, test-plane, or snapshot call and detailed cause had disappeared from routine evidence, forcing reconstruction from another agent's prose. | Every actual Coordinator call, including malformed, rejected, failed and successful calls, emits bounded structured received and terminal records at the relevant local RPC boundary with time, duration, caller attribution, native operation/request correlation, non-secret resource identifiers and a sanitized outcome. Internal test and snapshot calls preserve the typed stage, subject, exception class and errno before public error flattening. The shared JSONL log uses a multiprocess lock, rotates under a fixed byte and file-count ceiling, survives service replacement, is readable and writable by the trusted same-developer services, and never records raw request/result payloads, paths, credentials, environment values or secret-bearing arguments. A caller-side filesystem probe that never contacts Coordinator is not reported as a Coordinator failure; the absence of a correlated call is itself diagnostic evidence. | Exercise success, typed rejection, unexpected failure, malformed payload, capacity rejection, disconnected reply, snapshot dependency failure, concurrent processes and service replacement; prove paired operation/request correlation and useful path-free root-cause identity, explicit redaction, valid one-record-per-line JSON, deterministic rotation with no more than the configured files/bytes, and a production diagnostic read that finds freshly issued known calls at authority, test-plane and snapshot boundaries. |
| UIL-OPERATIONS-002 | Headless browser lifecycle | Agent and test browser process trees could outlive their caller, consume tens of GiB indefinitely, remain outside repository accounting, and require manual discovery because the Coordinator neither observed their activity nor owned idle cleanup. | Coordinator identifies only explicit headless/automation browser trees, measures their CPU, working memory and last observed work, attributes managed sessions when available, and reaps abandoned eligible trees after a bounded idle interval. Project-contained browsers stay in their project accounting, governed test browsers remain owned by their runner, PID reuse cannot target another process, and a release that first introduces this lifecycle performs one bounded all-headless cleanup immediately before activation. | Exercise activity and inactivity transitions, process churn, PID reuse, protected project/test cgroups, TERM-to-KILL escalation, respawn, bounded retained cleanup evidence, and release replay. Before production activation, prove current headless automation trees are removed; afterwards prove a managed canary appears in Performance, activity refreshes its timestamp, idle expiry removes its exact tree, and interactive browsers are never selected. |
| UIL-OPERATIONS-003 | First-use repositories and development servers | A valid newly initialized repository could not use the installed Coordinator because the static client rejected its root before contacting the healthy broker, and one production acceptance path later tried to traverse `.git` as the client account and failed with `EACCES` before any broker call. After authority adoption succeeded, the Console's long-lived protected client still used only its stale installed profile for a read-only log request, rejected the newly adopted repository locally, and raised a global 409 banner. Its later dynamic resolver still looked only for a target enrollment owned by the protected API account, so a repository correctly adopted by another trusted local account remained invisible. Other first-use failures were flattened into broker-outage or generic inspect-logs advice, filesystem stat owner was incorrectly used to choose the execution account, acceptance reused the application's ordinary port, and a nested application directory was mistaken for the Git root. A later adoption succeeded in authority storage but its client rejected the success reply because producer-only UID evidence had never been round-tripped through the strict repository parser. Production then used the right caller UID but only one compatibility GID even though the repository root and nested application directory used different groups; after adding both groups, systemd's own pre-exec `WorkingDirectory=` step still failed before the structured command could run, while `--quiet`, pre-catalog diagnostic loss, and a transport/broker-outage classification initially hid the definite launch rejection. The final launch then reached the actual caller but a third account's generated `0700` executable had collapsed the inherited POSIX-ACL mask to `---`, so the named caller entry existed but was ineffective. | Read-only discovery remains non-mutating and reports a valid unknown repository as unenrolled without client-side Git traversal or a false broker-outage claim. A protected non-executing reader proves repository identity. Every read-only control-plane action dynamically resolves an already authority-adopted repository through an existing current transport anchor, so a stale installed profile never blocks status or logs; when the protected caller has no target-specific enrollment, the resolver reuses an enabled, current routing policy belonging to another trusted local account without changing the physical caller UID. Absence of a per-account row is not a local authorization denial. Enabled policy UIDs within one account form a union; an account-wide block disables every matching enrollment or uses the exact repository/action/lifecycle revocation. This refresh cannot perform first-use adoption. The first typed start-like request atomically adopts the Git root, creates or refreshes routing/execution state for the actual non-root local caller, durably records that UID as both attribution and execution identity, and runs the structured no-shell command only as that caller. UID/GID/owner/mode/ACL metadata is diagnostic and routing/execution compatibility evidence, never local authorization; filesystem stat owner, root, and control-plane identities never substitute for the caller UID. The transient unit keeps the caller's primary GID and may add existing non-root filesystem GIDs along the resolved working-directory chain solely as per-unit supplementary compatibility groups, without changing account membership; group 0 is never added. For a fresh launch, exact-port preflight precedes a descriptor-anchored walk of only the validated cwd. The walk prunes `.git`, never follows symlinks, silently skips multiply linked regular files, and adds only group-class `rwX`/ACL-mask bits while preserving bytes, UID/GID, named ACL entries, world bits, and regular-file executable intent. A port collision performs no metadata mutation; there is no `UMask=0000`, broad source-root normalization, or world-access grant. Systemd starts one fixed Coordinator-owned credential-drop shim in `/`; the shim applies the caller identity before a trusted `env --chdir` boundary enters the validated repository cwd and execs the structured argv without a shell or elevated capability. The Coordinator proves all four Linux UID fields of the stable ready MainPID equal that caller and stops the unit if it cannot. Every failure identifies stage, broker contact, mutation, typed cause, retryability, and one exact next action, including bounded native stderr or journal evidence when systemd rejects a unit before it enters the runtime catalog. A definite systemd rejection is classified as a launch infrastructure failure even if secondary diagnostic collection fails. Local fallback stays disabled. Production acceptance uses a run-unique service and dedicated ephemeral test port, while canonical Git root and contained application cwd remain separate inputs. | In the software-owned delivery cycle, use a zero-commit repository with untracked source, an installed-but-stale profile, and a non-root caller whose UID differs from filesystem stat owner and has no prior routing record. Prove discovery makes no mutation and needs no client-side `.git` traversal, then perform one idempotent first-use start, fetch the strict-port server, and clean it up. Before replacing the installed profile, use the protected Console client with no target enrollment to resolve that authority-adopted repository through another enabled local routing policy, capture exact-resource logs without mutation or a global error banner, and prove the physical API caller remains the attributed peer. Then disable every matching enrollment for that route account and disable the exact runtime action; prove neither case falls through to another account's enabled policy. Round-trip the exact live `repository.ensure` reply—including paired attribution-only execution/filesystem-owner evidence—through the release's strict client parser before launch. Assert the command executes with the actual caller UID, keeps the caller's primary GID, adds every non-root existing filesystem GID needed across a mixed-GID nested cwd only as per-unit supplementary source compatibility, performs the validated cwd transition only after credentials are applied, retains the caller UID in operation evidence, proves the ready MainPID's real/effective/saved/filesystem UIDs, stops on any mismatch, and performs no group-0 grant, shell execution, chown, account group enrollment, named-ACL-entry edit, owner-derived UID selection, root execution, capability grant, control-plane execution, local fallback, or manual enrollment. Seed a third-account `0700` executable plus a regular non-executable, `.git` metadata, an external symlink, and a multiply linked file; prove only eligible cwd group-class mask/access bits change, executable intent and world bits remain stable, `.git` and external/multiply linked inodes remain unchanged, and no `UMask=0000` is emitted. Cover a systemd pre-catalog rejection and prove the persisted operation, installed client envelope, and rolling call journal retain a bounded sanitized native diagnostic and a `launch`/`infrastructure_failure` envelope even when journal lookup itself fails. Cover concurrent first use, lost-reply replay, linked worktrees, nested application cwd, path escape, explicit disabled action/lifecycle, port collision without hopping or normalization, broker unavailability, TTL cleanup, call-journal correlation, and normal Python/`python -O`. Require bounded stage/contact/mutation/retry/next-action evidence for expected and unexpected failures, and prove public identity and secret boundaries remain unchanged. |
| UIL-OPERATIONS-004 | Software-owned release replacement | A fully verified candidate was blocked before mutation because the retiring release contained interpreter-generated `__pycache__` files from a legacy launcher; a strict candidate-integrity verifier was incorrectly reused as a permission and cleanliness gate on the already-running predecessor. | Candidate releases remain strictly content-addressed and fully verified. Replacement derives the retiring release's one required transition capability from its digest-bound manifest inventory without checking local ownership, modes, or generated bytecode, and direct worker Python launchers disable bytecode writes. Generated cache residue in a predecessor never blocks a healthy candidate switch. | Package a strict clean candidate, add non-canonical bytecode with restrictive modes to the retiring fixture, prove capability planning and switch preparation proceed, and prove a malformed or digest-contradictory retiring manifest still fails. Run the complete software-owned delivery cycle and verify the live switch and post-switch health. |
| UIL-OPERATIONS-005 | Actual-caller working-tree preparation | The fixed exact-cwd normalizer was correct in source, but the first authority sandbox omitted a writable `/home` view and returned `EPERM`. The next fix added `ReadWritePaths=/home` while retaining `ProtectHome=read-only`; that separate mount still won and returned `EROFS`. The client initially mislabeled the definite launch-stage rejection as `broker_unavailable` and gave no host repair. | The root authority receives a writable `/home` sandbox view solely for fixed code-bounded exact-working-tree normalization across all trusted local accounts: `ProtectHome=false` makes the explicit `/home` exception under `ProtectSystem=strict` effective. Repository code never inherits root or that view. A missing or ineffective view, or `EACCES`/`EPERM`/`EROFS`, is a certain launch `infrastructure_failure` with path-free errno and exact authority-boundary recovery, never a broker outage or project defect. | The availability topology rejects either an authority unit without `/home` in `ReadWritePaths` or any authority `ProtectHome` value other than `false`. Unit tests inject `EPERM` and require typed actionable evidence. The complete live first-use acceptance must normalize a third-account `0700` Vite target, launch as the actual caller, fetch it, prove the caller UID, and clean it up. |
| UIL-OPERATIONS-006 | Out-of-band Coordinator bug intake | Coordinator failures were reported only through chat or the normal authority path, so the reporting channel could fail with the component being reported, duplicate reports lacked stable correlation, closed defects remained as synchronization noise, and reports could not be transferred safely between independent servers. | A bounded server-wide open-bug registry is independent of authority, API, broker, testd and repository enrollment. Trusted local accounts can atomically report/list/close structured defects without local ownership authorization. Presence in the canonical open directory means open; closing removes the exact file, with no closed row or tombstone. Identical local active reports deduplicate while retaining first/last seen and occurrence count. Records carry portable redacted reproduction steps, structured commands, repository name, correlations, and stable local-or-remote origin identity, but no secrets, private host paths, repository-presence assumption, or unbounded raw logs. Imported records never deduplicate into a local observation merely because their failure fingerprint matches. | With all Coordinator services/profile discovery disabled, exercise CLI, dedicated launcher and MCP report/list/close; prove concurrent same-fingerprint local reports converge, remote-origin imports remain distinct and idempotent across re-export, concurrent readers never observe partial JSON, exact close removes the physical record for every Console instance, malformed files are isolated, records/results stay bounded, and obvious credentials/private paths are redacted. Verify tmpfiles and Console sandbox wiring against the confirmed same-developer local-trust assumptions. |
