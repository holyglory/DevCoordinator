# User Issue Ledger: Testing

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-TESTING-001 | Universal test scheduling and worker execution | The harness treated repository-declared CPU, memory, and PID limits as admission authority and per-worker cgroup quotas, permanently queueing valid tests whose declarations exceeded an unrelated background-slice budget. | Admit tests from current host-available memory and learned target memory use. Do not gate or throttle tests by CPU, PID declarations, or per-worker CPU/memory/PID quotas; record actual peak memory and CPU use for later scheduling and operator evidence. | Exercise never-seen and historically measured targets against injected `MemAvailable` values, prove CPU saturation never rejects work, prove learned peak memory changes later admission, inspect transient unit arguments for absence of CPU/Memory/Tasks limits, and verify the Console/API report truthful memory-wait reasons and measured usage. |
| UIL-TESTING-002 | Governed test launch, timeout, and delivery acceptance | The generic 10-second broker request timeout covered synchronous snapshot materialization and transient-runner startup, and remained hidden in ticket issuance and status reads after the first fix. A lost launch reply was terminalized as an infrastructure failure with zero evidence even though the host operation could still be running; delivery acceptance never launched a real cross-account governed attempt; and later real attempts were blocked by root-only or cross-account parent traversal, a mandatory filesystem reflink, an observer that treated systemd's omitted transitional terminal fields as malformed, then an authority unit that could not write its own artifact store. The acceptance verifier later adopted a repository-scoped broker contract one command at a time, so submission worked while the following wait still failed parsing. | Keep caller-selected execution, launch, and waiting deadlines distinct from bounded internal transport retry slices. Every snapshot, ticket, launch, status, and cancellation RPC must consume the caller's remaining semantic budget without turning one slice timeout into a conclusion. Every run-scoped broker command must carry the exact repository ID returned by its validated plan. Launches must use a deterministic operation identity, converge after a lost reply without duplicate execution, and remain supervised while their outcome is uncertain. On this confirmed single-developer server, protected read-only source capture uses the fixed non-executing helper without depending on repository-owner UID traversal; actual test execution remains attributed to the repository owner. Fall back to an independent byte copy when copy-on-write cloning is unavailable, accept truthful transitional systemd observations, and declare every required protected-service data path in the deployed unit. | Delay each launch phase beyond one transport slice and prove exact replay returns one handle with one host start within the caller's total launch deadline; prove `test wait` returns at its caller-selected deadline even when status RPCs fail while the run remains supervised; prove exhausted reconciliation never produces a false terminal result; prove a repository behind an unreadable cross-account parent is still captured by the authority-bound fixed read-only helper while the runner remains owner-UID attributed; emulate no-reflink storage and transitional systemd fields; remove artifact-store creation or write access from the authority unit and require topology rejection; recover a transient artifact-store error without traceback spam; retain bounded native-manager failure evidence; require the acceptance verifier to assert exact repository binding on submit, wait, status, summary, and failure-detail calls; and require software-owned delivery to complete one real cross-account governed attempt with measured runtime evidence. |
| UIL-TESTING-003 | Native runner launch descriptor, immutable dependencies, and bootstrap evidence | The protected publisher upgraded launch evidence to schema 2 with a broker ticket while the trusted runner still accepted only schema 1, so every real runner exited before tests. The harness then reported only `infrastructure_failure`, with no failure row explaining the rejected descriptor. Later, pre-handle failures exposed zero evidence; systemd's transitional `deactivating` state was misread as a successful terminal exit before the result appeared; hidden cross-account interpreter targets failed at bootstrap; and immutable snapshots excluded ignored `.venv*` and `node_modules` roots while the launcher either selected a dependency-free system executable or derived one incomplete bind from `argv[0]`. The first dependency-binding repair compared incompatible snapshot hashes, ignored cross-account read modes, did not bind mutable interpreter/install identities, selected an incomplete owner NuGet cache, and compared a NuGet lock's logical `contentHash` with the different raw-archive `.nupkg.sha512` value, falsely rejecting every complete cache. Directly binding that developer cache still failed across local accounts and exposed unrelated packages; restoring from a copied package without its NuSpec and sidecars also failed. .NET repeated first-use/workload setup, imposed a hidden 30-second readiness deadline, buffered probe output without a bound, allowed implicit restore to require a remote NuGet source despite an exact bound cache, and flattened the runner's typed bootstrap outcome into an ordinary project failure. Finally, `manifest doctor` ran executable and path-access probes as the calling account, so a valid Coordinator-managed cross-account dependency mapping was falsely reported as an admission failure before protected planning was attempted. | The exact descriptor emitted by the protected publisher must be accepted by the same release's trusted runner, while unknown or malformed fields still fail closed. Every definitive failure before or during native launch must persist one bounded, actionable failure tied to the exact attempt, stage, error code, and available termination evidence before terminalization. Transitional native states remain supervised. For immutable runs, Coordinator—not repository mount declarations—derives normal ignored dependency roots from the target, binds their exact snapshot-lock and installation identity read-only at the same materialized paths, chooses the repository Python environment when declared, records and revalidates its exact external interpreter, and makes Node packages available without vendoring them. On the confirmed single-developer host, it may select a complete dependency cache and read groups across local developer accounts while homes remain hidden wholesale and writes stay scoped to declared paths. NuGet lock `contentHash` is verified against `.nupkg.metadata.contentHash`; the distinct archive SHA remains separately verified. Coordinator stages only the exact locked archives, NuSpecs, and identity sidecars into a runner-owned read-only hierarchical feed, then restores into an empty attempt-local package root; it never binds the developer's whole cache into the runner. Before immutable .NET tests, the runner resolves exactly one stable in-snapshot project/solution, performs one network-free forced locked restore against only that admitted feed, disables remote audit/build-server reuse, then runs `dotnet test --no-restore`; it does not run unconditional workload maintenance. The runner's typed terminal outcome is preserved through runtime observation and broker terminalization: cache/bootstrap faults are infrastructure evidence, caller-deadline exhaustion is timeout evidence, NU1004/NU1005 lock-graph drift is project setup evidence, and test failures remain test failures. Do not invent an artifact when no runner or native process existed. `manifest doctor` validates the repository contract and protected capability policy; it never treats caller-side `test -x`, path traversal, virtual-environment readability, or host-tool lookup as Coordinator admission evidence. Only protected planning/snapshot validation and an actual typed run result can establish those blockers. | Round-trip supported descriptor and result schemas from publisher through runner, runtime, and broker; reject contradictory ticket fields; exercise the release as the repository UID; and require deterministic bounded evidence before terminalization for ticket/materialization rejection, descriptor rejection, dependency-root, install-identity, interpreter-identity, cache-completeness or lock mismatch, process-bootstrap failure, and native startup exit without a runner result. Run real immutable Python and Node targets that import packages from cross-account ignored `.venv*` and `node_modules` roots; assert normal materialized paths, read-only binds, exact snapshot digest semantics, no whole-home exposure, no system-Python fallback, and rejection of missing, stale, escaped, substituted, or retargeted roots. Prove unrelated shared-cache changes do not invalidate the target. Exercise a clean-home .NET attempt whose raw archive SHA deliberately differs from a matching metadata/lock content hash; reject missing or stale exact-package metadata; require the staged feed's exact file set, caller-selected deadline, bounded probe streams, an empty runner-owned package root, exact relative-project cwd handling, forced offline locked restore followed by `--no-restore`, successful TRX, and distinct broker-visible infrastructure, timeout, lock-drift, and test-failure conclusions. Exercise `deactivating`, ingestion failure/replay, and uncertain post-handle launches as before. Prove doctor remains ready when a declared executable is unavailable to the caller but valid for protected dependency mapping, then require a real immutable plan/run to surface any dependency failure with a call-journal correlation, operation ID, run ID, and bounded Coordinator evidence. |
| UIL-TESTING-004 | Governed test planning and broker diagnostics | The planner emitted an unbounded reason per changed path, so a large dirty repository produced a plan that its own broker decoder rejected at 32 reasons. Validation still reported the manifest ready, and the broker flattened the useful cause into an opaque `test_contract_invalid` response without logging the operation-specific detail. | Every planner-produced document must satisfy the same bounded wire contract consumed by the broker, regardless of change count. Preserve representative reasons plus an explicit omitted count, and return and log a bounded actionable contract cause tied to the exact operation. | Generate plans with thousands of matching changes and round-trip them through the broker decoder and recomputation; prove reason count stays within the shared bound, the omitted count is deterministic, selection is unchanged, and a deliberately invalid contract yields a bounded cause and operation correlation instead of only a generic message. |
| UIL-TESTING-005 | Stable agent test attribution and merged protected-profile routing | The all-client protected-profile parser preferred a duplicate repository enrollment by expiry and low UID instead of the calling UID, so a GlobalFinance CLI request was routed through the reserved `devcoordinator-api` account. The stable client then sent its canonical `codex:` attribution through a broker field whose implementation accepted only a normalized `google:<email>` actor for that account; planning succeeded, but submission was rejected before run creation. | For a repository enrolled under multiple local clients, select the calling UID's exact current enrollment first and use another local enrollment only when the caller has none. Preserve one canonical local `codex:` actor namespace end to end for stable CLI submissions, while keeping `google:<email>` delegation exclusive to the protected Console API account and preventing either namespace from being interpreted as the other. | Parse a multi-client profile with duplicate repository grants whose API enrollment has the later expiry and prove the calling UID's account wins; prove deterministic cross-UID fallback when no calling-UID grant exists. Round-trip CLI `codex:` and fallback-UID actors through broker submission under ordinary and API-routed accounts, reject malformed or Google-shaped local actors, retain exact normalized Google delegation checks, and require an installed GlobalFinance enqueue to reach the test plane and return a run handle with correlated bounded evidence. |
| UIL-TESTING-006 | Development continuity during harness failures | Agents treated a Coordinator test-plane outage as a prohibition on all testing, blocking source development, or risked substituting an ungoverned local run for immutable shared evidence. | After recording a reproducible Coordinator bug, an agent may run repository-native isolated tests outside the harness so coding continues. Those results are explicitly local/advisory, never ingested into Coordinator statistics, never reused as handoff/release evidence, and never presented as proving the governed immutable source. Host-visible or shared-state tests still require Coordinator ownership. | Simulate authority, testd, profile and admission failures and prove the documented agent response files the bug then runs only isolated native checks. Static and forward tests reject claims of Coordinator evidence, release readiness, shared-runtime bypass or silent failure suppression; a later repaired harness run remains required for governed evidence. |
| UIL-TESTING-007 | Test-plane source and scheduler error taxonomy | A repository snapshot failure for one missing input was flattened by the authority into `test_scheduler_unavailable`, even though snapshotd and testd were healthy, so the report falsely described a shared outage and hid the exact repository setup correction. | Preserve repository-source and snapshot-materialization failures as bounded `test_plan_source_invalid` responses with a safe repository-relative path and an exact next action. Reserve `test_scheduler_unavailable` for actual scheduler/service transport failure; never tell an agent to wait for testd when its repository source contract is the blocker. | Inject missing, unreadable, changed, excessive and malformed snapshot inputs through the real test-plane transport and require source-specific public diagnostics plus operation/call correlation. Independently disconnect testd and prove only that case is classified as scheduler unavailable. Reproduce the original enrolled repository command after deployment and verify the public response names the missing input without disclosing host paths. |
| UIL-TESTING-008 | Software-owned installed-agent acceptance | The deployment runner executed every post-deploy acceptance command through its root-only mutation prefix. The installed Codex policy verifier therefore saw root, which intentionally has no per-user Codex installation, and falsely failed even though the actual developer account was configured. After moving journeys to the caller, Playwright session preparation could no longer read the private Console signing material and failed before the actual browser ran. | Package and host mutations use the privileged prefix. All installed-agent acceptance journeys—including Codex policy discovery, Coordinator client calls, governed probes and Playwright—run as the actual delivery caller so they exercise the same account and environment agents use. A narrowly separated preparation step may use the privileged prefix only to mint one short-lived acceptance cookie from private Console credentials and atomically hand that one mode-0600 file to the exact caller; the browser consumes and removes it. TLS keys, session secrets and other acceptance commands never cross that boundary. A privileged browser result is not a substitute for caller evidence. | Give the delivery runner a detectable privileged prefix and prove only the explicit session-preparation command receives it while every acceptance journey omits it. Verify the handoff file is exact-caller-owned, mode 0600, bounded, deleted after browser execution, and does not expose TLS/signing inputs. Run the complete delivery cycle from a configured developer account and require installed Codex policy, inventory, governed runner and production browser acceptance to pass. |
| UIL-TESTING-009 | Test capability policy | A validated target requesting no sensitive capability was blocked because the repository generation had no administrator-sealed capability row. After capability-free targets were repaired, ordinary private-loopback targets were still rejected even though confirmed single-developer policy treats isolated loopback as normal local test reachability. | The sealed capability registry governs only actual sensitive requests: host-loopback or external networking, sealed fixtures, and credential aliases. `none` and private `loopback` are admissible without a repository row, while exact repository identity/generation, manifest validation, snapshot provenance, and lifecycle checks remain mandatory elsewhere. Any sensitive request still requires an exact current repository-generation row and every named capability. | Exercise missing, stale, and current policy rows with capability-free, private-loopback, host-loopback, external, fixture, and credential requests. Prove `none` and private `loopback` authorize and report no missing capability, while every sensitive request remains default-deny without the exact current named grant. Reproduce both an enrolled capability-free target and an isolated-loopback change plan through the installed CLI and require plan/run creation. |
| UIL-TESTING-010 | Console browser geometry and performance gates under full delivery load | A real layout test sampled adjacent cells in separate browser operations while live refresh could replace the row between samples, producing a false missing-cell failure. A cached-shell TTFB gate included the first TLS connection and crossed 100 ms only under the complete concurrent source-check load, even though the focused journey passed and the product requirement concerns the cached Console shell. | Browser geometry evidence captures related elements atomically from one retained parent render. Cached-shell TTFB is measured on a reused same-origin connection while first-paint and LCP still cover the actual Console navigation; load never weakens the 100 ms TTFB or 1 s paint requirements. | Run the focused real-browser fixture and the complete software-owned delivery cycle. Force a row replacement between non-atomic reads and prove the atomic assertion remains truthful while persistent hidden or overlapping cells still fail. Add cold same-origin connection delay above 100 ms and prove it is excluded from the cached-shell TTFB sample, then add document response delay above 100 ms and prove the gate still fails. |
| UIL-TESTING-011 | Agent-local test scope and governed batching | Agents treated every isolated unit test as eligible for direct local execution, so a broad suite with more than one thousand cases could run locally instead of using the Coordinator's asynchronous dependency waves, shards, bounded summaries, and durable evidence. | Local test execution is narrow feedback only: one invocation may collect at most 20 cases and run for at most 10 seconds. If either bound cannot be established before launch, or collection would exceed 20 cases, use one policy-derived Coordinator enqueue and follow its run handle. Static checks remain local. A harness outage permits only the same bounded advisory tests after reporting the failure; it never permits a broad local suite. | Static skill and agent-guidance checks require the 20-case and 10-second limits, state that unit isolation does not imply local eligibility, route unfiltered or otherwise unbounded suites to one `devcoordinator test enqueue`, prohibit recreating a batch through repeated local commands, and apply the same limits to outage fallback. Documentation contract scenarios must keep a single explicit fast test local while routing 21 collected cases, an 11-second test, unknown scope, and a thousand-case suite to governed batching with bounded follow output. |
| UIL-TESTING-012 | Running-test recovery across testd replacement | A same-schema Coordinator delivery left a governed native test running longer than its 30-second scheduler lease. The replacement testd recovered the durable spool identity but attempted an ordinary heartbeat against the expired lease before observing the exact retained runtime, so the reaper abandoned live work with `running_heartbeat_lost`. A later repair recovered the lease but let snapshotd lifecycle-manage the shared attempt root at mode `0700`; restarting snapshotd removed traversal from the attributed repository UID, so a healthy runner could not publish its durable result and was falsely terminalized as a clean native exit without evidence. The defect recurred when a release replacement left an expired schema-baseline attempt natively running but absent from replacement supervision until cancellation reaped it as heartbeat loss. | A durably retained active attempt receives one bounded, generation- and owner-fenced recovery lease when testd reconstructs its exact spool/runtime binding. Recovery inventory is complete before the scheduler loop can reap or accept cancellation: every retained active spool is either attached to exact native observation and renewed, converged from complete result evidence, or terminalized with a typed contradictory/missing-runtime cause. The shared attempt root remains root-owned and traverse-only across tmpfiles application and every service restart, while per-attempt output remains owner-private; no service with a private state-directory mode may lifecycle-manage that root. The first post-restart observation must then renew or terminalize the attempt normally; a missing, contradictory, reaped, or terminal identity stays fail-closed and can never be resurrected or duplicated. | Advance a running and an uncertain-launch attempt beyond the ordinary lease, replace authority and testd, and prove complete spool enumeration attaches and renews both before any reaper or cancellation turn while no second launch occurs. Reject stale generation, wrong lease owner, absent spool, contradictory runtime, already reaped and terminal attempts with typed evidence rather than silently omitting them. Stat the shared attempt root before and after snapshotd/testd replacement, require mode `0711`, and prove an attributed repository-UID runner publishes and drains its exact result. Exercise a same-schema replacement during a real governed test and require its original run handle to reach the runner's truthful terminal result without heartbeat-loss or native-result-loss evidence. |
| UIL-TESTING-013 | Public open-bug completion audit | An agent reused a local zero result and an earlier Bugs-page screenshot as proof that the current public Console collection was still empty, even though new reports had arrived afterward and were visible at the production destination. | A zero-bug completion claim requires one fresh, authenticated read from the exact production `/api/bugs` authority used by the public Console plus a rendered `#/bugs` check from the same deployment. Local CLI output, cached state, or earlier acceptance evidence is insufficient. Every returned report remains open until its stated defect is reproduced, fixed, deployed, verified through its original surface, and then closed by exact bug ID. | Add reports after an earlier zero observation and prove the completion audit detects them. In release acceptance, bind the authenticated API response and rendered page to the same base URL/release, require both counts to be zero, and fail on stale evidence or any report. For a repair batch, preserve the initial production report set, verify each fix against its reproduction, close only those exact IDs, then repeat both fresh production checks. |
| UIL-TESTING-014 | Immutable snapshot source churn and diagnostics | An ephemeral untracked SQLite sidecar disappeared between Git enumeration and protected capture, so an otherwise valid immutable run was rejected. A separate invalid manifest returned only a generic inspection failure instead of the exact repository-relative field error. Later a large actively edited repository changed between the before/after materialization scans; the first attempt failed without one complete materialization retry, and its exact `retry from a fresh plan` cause missed the public allowlist and became the same generic inspection message. | Immutable capture retries one complete bounded scan and one complete materialization when repository content changes or an enumerated path disappears; permanently missing tracked inputs still fail. Manifest JSON, contract, and source-churn failures retain one bounded safe field, syntax diagnostic, repository-relative path, or explicit stable-source retry action, while public output never exposes an absolute host path. A preview-local `not_found` is classified as source/plan state, never a scheduler outage. | Race creation/removal of untracked SQLite `-wal`/`-shm` files through protected immutable planning and between the copy's before/after scans; prove capture either includes a stable file or retries the entire materialization once to a stable tree, cleans the abandoned stage, and does not reuse its stale snapshot ID. Keep a tracked missing file and persistent churn as deterministic failures. Inject malformed JSON, missing fields, unknown fields, invalid target values, the exact `retry from a fresh plan` message, and preview-local not-found; require the exact safe cause through the installed client without `test_scheduler_unavailable`. |
| UIL-TESTING-015 | Governed artifact content retrieval | A governed run retained an integrity-verified process-log artifact behind a root-only opaque handle, but the stable artifact command returned metadata only, leaving the report diagnostically unusable to the attributed agent. | Resolving an exact artifact verifies its immutable handle, size and full digest inside testd, then returns a bounded content tail for textual artifacts with explicit full-size and truncation evidence. It never exposes a private host path, returns unverified bytes, or exceeds the model-facing result bound. | Store textual, binary, multibyte, empty, changed, oversized and missing artifacts. Prove textual tails are integrity-verified and retrievable through the installed command, binary content remains metadata-only, truncation is explicit, identity contradictions fail closed, and every result stays within both test-plane and agent bounds. |
| UIL-TESTING-016 | Long-running governed attempt liveness | A valid long-running attempt held its exact-worktree lease while dependent targets reported only `exact_worktree_busy`; repeated follow calls showed the same completed-target count and gave no evidence that the active attempt was still heartbeating toward its declared deadline. | Run status exposes bounded active-attempt liveness for each running target, including exact attempt identity, start, last heartbeat and lease expiry. The model-facing follow projection includes representative active attempts, so an advancing heartbeat is visible progress while a lost heartbeat still terminalizes through the existing lease/recovery contract. | Hold one attempt active while two same-worktree targets wait, advance heartbeats without completing a target, and prove successive status/follow results show advancing liveness within their byte bounds. Then stop heartbeats and prove the normal lease-expiry terminal evidence appears without duplicating or silently releasing the active attempt. |
| UIL-TESTING-017 | Governed follow continuity across control-plane replacement | A same-schema release briefly restarted testd while another developer followed an already-enqueued run. One transient `test_scheduler_unavailable` response ended the bounded follow and was reported as a lost run even though the durable run and active attempt remained healthy after replacement. The defect recurred when one active schema-baseline follow exhausted its outer deadline while individual status observations consumed long transport slices, leaving no final bounded decision despite a retained run. | A bounded wait/follow treats scheduler transport unavailability, broker maintenance, saturation, connection reset and timeout as transient read failures until its caller-owned deadline. Every nested status call is capped by the smaller of its transport slice and the caller's remaining budget, leaving a reserved response margin for one final decision. It preserves the last valid status, never resubmits or duplicates the run, and returns the truthful terminal or timed-out result once authority recovers. Direct zero-wait status remains a single observation. | Inject each allowlisted transient before a healthy or terminal status and prove one follow recovers without enqueueing. Make status calls consume their full slices near the outer deadline and require follow to stop starting reads at the reserved margin, then emit one bounded timed-out document with the last valid status when available. Hold the outage through the deadline and require the same result. Exercise a real governed run across same-schema replacement and require the original handle and active-attempt liveness to survive. |
| UIL-TESTING-018 | Post-deploy installed test-access acceptance | The immutable repository discovery probe retained the generic ten-second deadline first in the public broker client and then in its nested broker-to-testd hop, so a healthy freshly restarted control plane could return `request_timeout` under normal post-deploy load and make an otherwise healthy release intermittently fail acceptance. The acceptance command's apparent catalog journey actually routes through repository setup before returning the catalog. | Carry bounded release-recovery read deadlines for both repository setup and catalog through client-to-broker and broker-to-testd transports. Preserve a typed failure when either deadline is genuinely exhausted; never skip the real installed-agent probe or turn a timeout into success. | Delay setup and catalog responses beyond ten seconds independently at each transport hop and prove each request continues within its explicit read deadline, retain transport-call contract coverage for the new optional timeout, then require the complete software-owned delivery cycle to pass the installed cross-account test-access acceptance under concurrent post-deploy checks. |
| UIL-TESTING-019 | Cross-account immutable cache selection and submission | An immutable plan attributed execution to one local account and searched only that account's cache; a later source-owner fallback still failed after ordinary compatibility metadata made filesystem ownership deliberately non-authoritative. The exact locked packages and external Python toolchain existed under other trusted developer accounts. Separately, submit retained the generic ten-second transport deadline and could lose a valid run handle during post-restart recovery load. | On the confirmed single-developer host, dependency discovery considers every configured non-root interactive account whose home is directly under `/home` as a non-authorizing cache/toolchain candidate, deduplicates them, and still stages or binds only the exact lock-verified dependency/toolchain artifacts into the attempt. Filesystem owner never selects access or execution. Test submission has its own bounded recovery-load transport deadline and preserves the exact durable run handle. | Use distinct caller, source filesystem owner, package-cache owner, and external-toolchain owner identities; prove exact immutable Python and .NET selection succeeds without exposing a whole home, authorizing by ownership, or admitting system/noninteractive accounts. Delay submission beyond ten seconds but within its explicit deadline and prove one exact handle returns with no duplicate run. |
| UIL-TESTING-020 | Nested Coordinator calls from immutable attempts | Repository automation ran from an authority-owned immutable snapshot, then its nested installed Coordinator command treated the snapshot path as a new unenrolled repository because the sealed tree intentionally contains no `.git` metadata. A first repair still treated local owner/mode and original-root traversal as admission gates, so a valid cross-account route failed before contacting the broker. Later repairs reached the correct repository but changed legacy inventory's authority transport label and then exposed the broker's schema-v3 envelope to automation written against the stable v2 lifecycle contract. The failure recurred as multiple installed GlobalFinance immutable attempts terminating at their first inventory call: schema v2 omitted its required empty membership-list shape, then returned the authority-routing original root where automation required its effective materialized execution root. The surrounding diagnosis also misclassified the attempt from an unrelated caller-side `broker_transport_forbidden` result. | Every immutable snapshot publishes one read-only, fingerprinted, non-secret route beside the sealed tree that binds snapshot ID, repository ID, original canonical root, materialized root, and content identity. Stable agent and legacy lifecycle clients recognize that route before Git discovery and use the original root only as non-authorizing authority-routing context. The v2 compatibility projection exposes the materialized root as its effective `project` and matching repository root while retaining the original root explicitly as routing provenance; schema v3 remains canonical and unchanged. Explicit absolute broker-known roots are opaque routing identities. Local UID, GID, owner, mode, ACL, account membership, and path traversal never authorize or reject the route; malformed or contradictory content still fails. The legacy lifecycle inventory surface retains its authenticated Unix-socket label, schema-v2 envelope, and required empty compatibility collections while carrying additive current graph fields. Attempt failures preserve the actual nested call code and bounded diagnostic; an external caller's transport result never substitutes for attempt evidence. | Materialize and reuse a snapshot under a restrictive umask, resolve nested paths through both client surfaces, and prove the authority-routing original root, effective materialized root, repository identity, and content identity match while the binding remains outside repository code. Change owner/mode and make the original root locally inaccessible, then prove routing still reaches the broker. Reject malformed fields, wrong fingerprint, wrong snapshot/root identity, path escape, unrelated directories, and missing or duplicate authority repository rows. Assert the legacy inventory envelope remains v2 with authenticated local transport and empty membership compatibility while schema v3 contains no membership state. Run a dedicated installed immutable nested-inventory canary that preserves its exact failure code, then rerun the installed GlobalFinance immutable integration target through its nested ephemeral lifecycle. |
| UIL-TESTING-021 | Asynchronous enqueue acknowledgement and queue observability | Test planning performed immutable snapshot work synchronously before emitting any caller-visible identity, so healthy large repositories left agents with empty output for tens of seconds; before a run handle existed there was also no bounded queue/scheduler status surface. | Enqueue immediately emits one replay-safe typed acknowledgement with its exact operation identity and copyable continuation, then returns the eventual exact plan/run result without duplicating work. A read-only queue-status command works without a run handle and truthfully separates planning/snapshot, scheduler wait, active execution, current blockers, and memory-based capacity with bounded repository and fleet counts. | Delay snapshot preview beyond the ordinary caller wait and prove acknowledgement arrives first, exact replay returns one plan/run, stdout retains its single final JSON contract, and interruption leaves a copyable retry. Populate queued/running/waiting work across repositories and prove queue status reports bounded counts, approximate repository position, blocker samples, active attempts, and unknown capacity fields honestly. |
| UIL-TESTING-022 | Failed automation diagnostic selection | A failed automation target retained its declared diagnostic directory, but the failure record linked only process stderr; a harmless build warning occupied stderr while the actionable database error existed only in a declared log, so exact artifact retrieval omitted the cause. | On nonzero automation exit, the runner derives one bounded text diagnostic from safe text files in already secret-scanned declared directory artifacts and links the failure to it. Process captures and the complete directory remain retained; binary files, unsafe paths, oversized entries, and credential material never enter the model-facing diagnostic. | Fail an automation target whose stderr contains only a warning while a declared directory contains the real `.log` cause plus binary and oversized files. Prove the failure links the bounded diagnostic with the real cause, excludes binary/credential material and private paths, remains integrity-verifiable, and falls back to stderr/stdout when no safe declared text exists. |
| UIL-TESTING-023 | Immutable Python native dependency readability | The scheduler selected the intended locked `.venv-v2`, but mounted its original cross-account filesystem modes directly into the immutable attempt; a native extension owned by a compatibility identity lacked caller-read permission and failed with `EACCES`. A first repair tried to copy the environment inside unprivileged testd, which could not read the same source file it was meant to make usable. | Root snapshotd copies the already lock- and identity-validated Python environment into a root-owned non-secret dependency directory beside the exact immutable snapshot, normalizes the copy for read/traverse access, records its immutable device/inode identity, and publishes only that read-only copy to testd. testd revalidates both the authoritative source and staged identities before mounting. Original repository modes and ownership never authorize, reject, or get mutated. | Create a lock-bound environment with a native extension that testd cannot read but snapshotd can. Prove root staging succeeds, the published copy is readable, source metadata is unchanged, the read-only unit bind uses only the staged snapshot path, and native import succeeds. Reject a missing stage, path outside the exact snapshot, source substitution, lock drift, staged root substitution, marker or installation-identity tamper, unsafe file type, and incomplete copy before launch. |
| UIL-TESTING-024 | Multi-project .NET result evidence | The trusted runner forced every project in one `dotnet test` solution invocation to write the same `reporter.trx`, so a later project overwrote earlier failed-case output. The run then reported a failing process with zero failed cases, pass counts from only the surviving file, and no assertion cause. | A .NET invocation may publish multiple bounded TRX files. Every exact file is parsed once, case identities remain unique across projects, failed/error results retain their bounded message and stack/output cause, aggregate counts cover the whole invocation, and the process failure links to the reporter artifact that carries it. | Emulate two test projects writing distinct TRX files where the first fails with an assertion and the second passes. Prove both files are retained, counts are 1 failed/1 passed, the failure names the assertion cause and exact case, duplicate or excessive reporter files fail closed, and a single-project invocation remains compatible. |
| UIL-TESTING-025 | Sealed fixture launch diagnostics | Fixture provisioning caught a typed host/container failure, cleaned the partial fixture, then replaced the useful cause with `test fixture provisioning failed`; the run exposed only that wrapper and no artifact, so the exact correction could not be determined. | A failed sealed-fixture launch retains one bounded, secret-redacted cause code and diagnostic from the trusted provider through broker, scheduler, run failure, and model-facing summary. Partial resources are still cleaned exactly; cleanup failure is appended without erasing the provisioning cause. | Inject image absence, create/start/namespace observation, credential publication, cleanup, and secret-bearing failures. Prove each run has a bounded safe cause and correlation, secrets/private paths are absent, cleanup executes exactly once, and generic unexpected exceptions remain typed without traceback leakage. |
| UIL-TESTING-026 | Exact dependency scheduling after a target failure | The scheduler encoded only topological wave numbers and treated every earlier-wave target as a dependency of every later-wave target. One unrelated fixture failure therefore cancelled an independently runnable server-test branch. | Admission and reconciliation use each target's exact declared dependency set. A failed dependency cancels only its transitive dependents; independent targets continue, and the run terminalizes only after every runnable independent branch has produced evidence. | Build two branches in overlapping waves, fail one root, and prove only its descendants cancel while the other branch runs to completion. Exercise shards, transitive dependencies, retry, queue-status blocker counts, restart reconciliation, and legacy retained plans. |
| UIL-TESTING-027 | Oversized structured test reports | A multi-project .NET run produced a valid TRX larger than the raw-artifact retention bound. The runner rejected that one file before parsing any remaining reports, retained only process-level failure names, and again omitted the failed cases and assertion causes. | Process each generated report independently. Keep the raw-artifact cap, but stream a strictly input-bounded oversized TRX into bounded case and failure records and a compact integrity-bound diagnostic artifact; one oversized or invalid report never suppresses other safe reports. | Generate small, oversized, excessive, malformed, and symlinked multi-project TRX sets. Prove oversized valid input preserves exact failed cases, assertion messages, locations, and a bounded derived artifact without retaining the oversized raw file; invalid input is isolated, other reports remain usable, input/case/failure bounds hold, and no private paths or unbounded output enter summaries. |
| UIL-TESTING-028 | Failed-only retry dependency normalization | Retrying the only failed target copied its original later-wave index but omitted its already-succeeded dependency from the new run. The scheduler then blocked the target forever on a dependency wave that the retry could never complete. | A retry derives a new exact dependency graph from the selected retry targets and the prior terminal outcomes. Already-succeeded omitted dependencies are treated as satisfied; selected dependencies retain their exact edges; every resulting wave is dense from zero, and no target may wait on an absent node or empty prior wave. | Retry a failed downstream target whose dependency succeeded, a failed dependency with a cancelled descendant, multiple independent branches, and a sharded target. Prove the retry starts, exact required dependents are selected or satisfied, wave indexes are dense, queue status has no phantom `dependency_wave`, replay is idempotent, and restart reconciliation preserves the normalized graph. |
| UIL-TESTING-029 | Immutable launch catalog for derived retries | Dense failed-only retry correctly derived a smaller plan, but immutable snapshotd looked only for a catalog file named by that new plan ID. The snapshot already held the exact launch description under the source plan ID, so the corrected retry failed before launch with `snapshot_failed` and a missing derived catalog path. | For an immutable retry plan, snapshotd may derive and atomically publish the missing exact catalog only from bounded existing catalogs under the same immutable snapshot whose source, manifest, repository generation, owner identity, target resources, and selected launch descriptors agree. Functionally identical sources collapse deterministically; missing, changed, unsafe, excessive, or materially ambiguous sources fail closed. | Publish an original two-target immutable catalog, derive a one-target failed-only plan, and prove first resolve creates the exact derived catalog and launches while replay reads the same bytes. Exercise multiple identical source catalogs, conflicting launch/resource/owner/generation records, absent targets, unsafe files, excessive candidates, catalog restart, and source-plan mismatch; require typed failure without reading another snapshot or repository. |
| UIL-TESTING-030 | Registered-plan recovery and bounded failure pages | Testd durably retained an immutable plan but kept its admitted target resources only in process memory, so a replacement between preview and submit silently substituted default worktree and sharding values and every first-wave launch failed `snapshot_contract_invalid`. A separate stable failure-index request returned enough individually bounded diagnostics to exceed the aggregate 8192-byte agent result contract. After paging was bounded, successful failure-index and retry replies still omitted `ok: true`, so the stable client emitted valid results with a failure exit status. | Plan registration durably binds the exact normalized target resources with the plan without changing the retained store schema, and a replacement restores those resources before submission. Stable failure pages preserve order and complete individual diagnostics but return only the largest prefix that fits the aggregate agent-result bound, with the last returned failure as the continuation cursor whenever more records remain. Every successful read or mutation envelope explicitly reports `ok: true` and exits successfully; the run conclusion remains a separate field. | Register an immutable plan with non-default materialized roots, shards, retry limits, estimates, and exclusive resources; replace testd before submit; and prove the queued targets retain the exact normalized values and launch against their matching snapshot catalog. Request multiple maximum-sized failures through the stable agent command, prove every response remains at most 8192 bytes, page through every record without gaps or duplicates, and preserve a one-record page plus existing server cursors. Exercise successful failure-page and retry calls whose broker payload omits `ok`; require an explicit successful envelope and exit status without changing a failed run's conclusion. |
| UIL-TESTING-031 | Terminal runner evidence, cancellation, and exact-worktree lease release | Multiple test runners published complete timeout/test-failure chunks, but a lingering or obsolete native cgroup remained active across Coordinator releases. Testd treated only native inactivity as terminal, kept renewing the completed attempts for hours, reported their runs as running/cancelling, ignored cancellation, and blocked unrelated queued work with `exact_worktree_busy`. A later large server suite exposed a second path: testd imported only one retained result chunk per supervision heartbeat, projected empty live counts, and could spend many minutes reporting an already-exited runner as active while hundreds of bounded chunks waited. | An atomic, identity-validated runner result with its complete chunk manifest is authoritative terminal evidence even if the exact native cgroup still reports active. Testd drains a bounded batch of consecutive result chunks per heartbeat, publishes current attempt/case/failure counts and timing while nonterminal, then consumes the exact terminal envelope and releases the worktree lease without waiting one heartbeat per chunk. Cancellation first imports an already-published result and preserves its truthful conclusion; otherwise it stops and terminalizes the bound attempt. Release recovery performs the same convergence and never renews an obsolete completed worker. | Publish success, test-failure, timeout, infrastructure-failure, incomplete, malformed, partial, stale-generation, contradictory, 65-chunk and maximum-chunk result streams while the exact unit remains active/deactivating. Prove bounded batching preserves order and one final marker, live status shows truthful counts/timing, only a complete matching result triggers exact-unit cleanup and terminalization, cancellation cannot overwrite a completed outcome, replacement recovery clears stale workers, and previously blocked same-worktree targets become runnable without duplicate launch. |
| UIL-TESTING-032 | Stable follow output and live failure projection | A bounded follow invocation exited successfully with no decision output, and live status reported zero failures even though cursorable failure records already existed for the active attempt. | Every successful follow path emits exactly one non-empty bounded JSON decision with `ok: true`, current state, timeout flag, continuation and next command. Live status projects the current retained failure-record count independently of terminal attempt aggregates, so it can never claim zero while the failure index is populated. | Exercise zero-wait, bounded timeout, transient recovery, terminal, live-with-failures, serialization fallback, and process-main emission paths. Capture stdout and exit status, require exactly one complete document for success, and compare the status failure count with the same store's failure page before and after terminalization. |
| UIL-TESTING-033 | Explicit release and handoff submission | Stable enqueue documented `release` and `handoff` as plan-review journeys but unconditionally submitted every preview, creating an unexpected governed run before the caller reviewed the immutable plan. | `change`, `checkpoint`, and `manual` may preview and submit in one invocation. `release` and `handoff` stop after registered immutable plan creation, return a `dc1:plan` continuation and exact submit command, report `submission_performed=false`, and never call submission. | Exercise fresh and replayed previews for every intent. Require no submit call or run identity for release/handoff, one deterministic submit for routine intents, explicit subsequent submit producing one run, and installed release enqueue leaving the selected plan registered but unsubmitted. |
| UIL-TESTING-034 | Cross-account immutable launch catalog identity | A stable immutable snapshot and deterministic plan were previewed by a second trusted local account, but the snapshot catalog payload embedded the planning UID even though launch content was otherwise identical. The fixed snapshot/plan path then rejected the harmless account difference as `snapshot launch catalog identity collided` and exposed only generic `test_plan_source_invalid`. | Immutable launch catalogs contain only plan/source, repository generation, launch, resource and provenance facts that determine execution. Planning/calling UID is attribution, not catalog identity. Existing catalogs with a legacy `owner_uid` field compare and load through an owner-neutral compatibility projection; materially different launch content still collides. | Publish the same snapshot/plan/launch catalog from two UIDs and require byte-stable owner-neutral reuse, including a legacy on-disk catalog from the first UID. Change argv, environment, fixture, dependency/resource or provenance content and require collision. Preview the installed GlobalFinance release plan from distinct local accounts and require the same plan handle without source-invalid or broker-unavailable classification. |
| UIL-TESTING-035 | System-Python-backed immutable virtual environments | Immutable dependency planning accepted only external Python executables located below a trusted local account home. A normal venv whose `bin/python` pointed to the host's `/usr/bin/python3` symlink therefore failed as `snapshot_python_dependency_unsafe`, even though the final interpreter was a regular executable in the service-owned system toolchain. | A venv may bind an absolute Python link through a fixed service-owned system toolchain root when the complete link chain resolves to a regular executable there. Planning and launch retain the exact link text, resolved executable, root device/inode, and bounded installation identity. The host system root is already visible inside the isolated unit and is never copied into PrivateTmp; account-home toolchains retain exact staging. Other absolute roots, relative escapes, non-regular finals, and changed links remain rejected. | Build a venv link through a version alias in an allowed system root and require immutable planning, launch validation, and direct system-interpreter selection without a staging bind. Change either link, final executable, device/inode, or installation identity and require rejection. Preserve staged account-home toolchains and reject arbitrary external roots. Run the installed XFoilFOAM `node-suite` target beyond snapshot dependency resolution. |
| UIL-TESTING-036 | Retry behavior after live source changes | Failed-only retry copied a terminal live plan's old source fingerprint into a new queued run. If the worktree had changed—as is normal after fixing failures—the new run immediately superseded before executing, even though the retry command had reported successful enqueue. | A retained live-source run is not replay-safe after source change. The stable retry surface must reject it before creating a run and direct the caller to create a fresh current-source plan; immutable retries retain exact failed-only execution. It must never report a queued retry that deterministically supersedes on its first source check. | Retry terminal live and immutable runs with unchanged and changed worktrees. Require live retry to create no run and return a typed replan-required correction, immutable failed-only retry to execute its dense exact dependency graph, and operation replay to remain idempotent without phantom runs. |
| UIL-TESTING-037 | Explicit live-change completeness | The legacy plan surface accepted caller-supplied `--change` paths as a selection override while the live content fingerprint still covered every dirty file. Omitting another real dirty path therefore registered a plan whose selected work and source identity described different change sets; launch failed later as `snapshot_conflict` instead of rejecting the contradictory plan. | Explicit live changes must exactly equal bounded Git discovery at planning time, including status, rename source, and untracked paths. A mismatch is a non-mutating typed planning error directing the caller to omit `--change` for protected discovery. No incomplete explicit selection may be registered or submitted. | Create modified, added, deleted, renamed, and untracked changes; require exact explicit coverage to produce the same plan as discovery, then omit/add/change one path or status and require rejection before broker registration. Preserve immutable broker preview and launch a correctly registered XFoilFOAM change plan without snapshot conflict. |
| UIL-TESTING-038 | Planner/decoder dependency canonicalization | Manifest validation accepted a valid schema-3 target graph, but planner output preserved declaration order for exact dependency arrays while the shared plan decoder required sorted canonical arrays. A doctor-green manifest with an unsorted multi-dependency target was therefore rejected later as `snapshot_contract_invalid: plan exact dependencies are invalid`. | Planner-owned exact dependency arrays are unique and canonically sorted before fingerprinting, serialization, execution-fingerprint derivation, registration, retry projection, and decoder round-trip. Manifest declaration order never changes plan identity or turns a validated graph into an invalid plan. | Build equivalent manifests with every dependency permutation, including manual bidirectional closure, and require identical plan IDs, waves, dependency maps and successful decoder round-trips. Preserve rejection of duplicates, self-dependencies, absent targets and wrong-wave edges, then reproduce installed DesignDocEngine manual planning without source-invalid classification. |
| UIL-TESTING-039 | Nonterminal automation progress | A long multi-stage `web-verify` automation attempt remained natively active for more than twelve minutes after its accounting phase, while follow could expose only a fresh heartbeat and zero terminal case counts. The caller could not distinguish ongoing output, a quiet valid phase, or a hung child before cancelling the run. | Every active attempt exposes bounded monotonic liveness evidence independent of terminal test cases: elapsed wall time, current attempt count, last heartbeat, current memory when available, and a privacy-safe output-progress signal that advances when captured stdout/stderr grows. No source text or unbounded log content enters status. The execution deadline remains enforced and terminal evidence still owns pass/fail counts. | Run quiet, steadily verbose, bursty, hung, cancelled and normally terminal automation processes. Require bounded output byte counters/timestamps and elapsed time to advance truthfully, remain stable while quiet, survive testd replacement, never expose output content, and disappear into measured terminal counts. Reproduce installed DesignDocEngine `web-verify` through build/accounting/Vitest or its declared timeout with no zero-information heartbeat loop. |
| UIL-TESTING-040 | Output progress beyond artifact retention cap | The runner correctly kept draining a verbose child after retaining the first 4 MiB, but nonterminal status measured only the bounded artifact file. `stdout_bytes` froze at exactly 4 MiB and `last_output_at` stopped, falsely making a live 5.8 GiB server test look stalled until cancellation. | Capture threads continuously drain each pipe and atomically publish a content-free progress sidecar containing total observed bytes, retained bytes, truncation state and last-output time. Status reports observed and retained counts separately; observed bytes continue monotonically beyond the artifact cap while retained bytes remain bounded. Sidecars never contain output text and are removed with attempt state. | Stream more than 4 MiB on stdout/stderr, prove the child cannot block on full pipes, the retained artifacts stop exactly at the cap, observed counters and timestamps keep advancing, truncation flags become true, and terminal digests cover the complete stream. Exercise quiet, capped, secret-redacted, replacement and malformed-sidecar cases. |
| UIL-TESTING-041 | Retained progress schema compatibility | An accepted run retained nonterminal progress using the initial five-field event shape. After deployment expanded progress into observed/retained/truncation fields, status required only the new shape, rejected the entire run as `invalid_request`, and the authority mislabeled that retained-data read failure as scheduler unavailable. | Retained initial progress events load through an explicit bounded compatibility projection that derives retained counters and honest unknown truncation from the facts the old schema recorded. New writes use the expanded schema. Malformed or contradictory events still fail closed, but one legacy event can never make an accepted run unreadable across a same-schema release. | Retain active and terminal runs with initial, expanded, malformed and mixed progress events; replace testd/authority; require status, follow, summary, cancel and artifact reads to preserve exact run identity. Confirm initial cap-equal counters remain non-truncated/unknown rather than inventing unseen bytes, while expanded events preserve observed overflow and truncation. Reproduce the reported run through the installed status surface without scheduler-unavailable classification. |
| UIL-TESTING-042 | Atomic result-stream and cancellation convergence | Server runners published complete multi-chunk measured results while their native cgroups were still active. The broker stopped the cgroup after returning the first chunk, and the stop path deleted the remaining result files; subsequent observations therefore regressed to `running`, so timeout/cancellation could renew the attempt and worktree lease forever. A cancel reply could also expose an operation handle that stable follow could not resolve. The opposite failure then appeared: after cancellation began, a large atomic result stream kept the cgroup alive for minutes because every `result` observation bypassed the stop retry until all chunks had drained, exceeding the unit's 30-second stop bound. A later runner retained 592 green cases, then exited without its final result document; the native-exit fallback assumed no prior chunks, rejected the valid partial stream on every heartbeat, and kept a 4 GiB cancelling attempt leased for more than ten hours. | An atomic result manifest is terminal evidence and proves its chunk files were published before the manifest. Ordinary supervision preserves the exact files and native binding until every declared chunk has been returned in order. Once cancellation or supersession is requested, the first validated manifest observation immediately requests exact cgroup stop without collection or materialization deletion, retries that bounded stop while chunks drain, and still preserves the measured result as terminal truth. If native exit loses the final result document after any bounded prefix was already retained, the fallback appends exactly one deterministic infrastructure record at the next chunk index and terminalizes with the complete retained prefix; it never assumes an empty stream or renews the same contradiction. Manifest-complete observation performs collection and emits the terminal envelope. Stable cancellation returns a durable resolvable operation or an honest typed unresolved result, never an unusable handle. | Publish zero, one, 65, and maximum bounded chunks while the exact cgroup remains active, then remove the final result document before native exit at every prefix. Require one additive infrastructure record, exact chunk order, retained cases, terminal attempt/run state, released worktree/exclusive leases, and no later heartbeat renewal. Under ordinary supervision require no native stop before the last chunk. Under cancellation/supersession require stop on the first validated manifest, retry while native state remains transitional, cgroup inactivity within `TimeoutStopSec`, every chunk returned once in order after stop, and measured terminal outcome precedence. Prove stop never collects or deletes unread chunks. Cover cancellation, lost reply, replacement, and stale-cgroup cases, then reproduce the retained affected runs after deployment. |
| UIL-TESTING-043 | Durable target execution deadlines | Testd delegated target timeout enforcement entirely to the native runner/systemd unit. When the native runtime stayed observable as active past the caller-selected target ceiling, testd kept heartbeating and renewing the attempt lease indefinitely, blocking later dependency waves until an unrelated authority replacement let the lease expire as abandonment. | Every native attempt exposes a durable start time from its root-owned launch evidence and the exact target TTL from its descriptor. Broker observation compares the independent authority clock on every heartbeat and recovery pass; at or after the inclusive deadline it first preserves any complete published result, otherwise performs exact native cancellation and reports timeout only after stopped proof. An active runtime without durable start evidence fails observation and cannot receive another lease heartbeat. Same-schema recovery uses launch-file time as a conservative non-extending legacy origin. | Exercise quiet, output-producing, result-racing, cancellation-racing, lost-reply, authority/testd replacement, stale-active-cgroup, legacy launch evidence, missing start evidence, and clock-boundary attempts with distinct per-target and plan override ceilings. Require native cancellation at the inclusive deadline, measured result precedence, `timed_out` rather than abandonment, released worktree/exclusive leases, cancelled dependent waves according to run policy, and no post-deadline heartbeat renewal. Reproduce the two reported release runs after deployment. |
| UIL-TESTING-044 | Complete and bounded large-run failure access | A large reporter retained every failed/error case count but capped detailed failure records at 64, so terminal summaries named more failures than any agent could retrieve. Separately, advanced failure reads could return many individually bounded diagnostics whose aggregate exceeded the 8192-byte agent envelope, forcing repeated failed requests instead of useful cursor progress. | Every retained failed/error case has one exact durable failure record up to the existing case/report/chunk bounds; process-level failures remain additive. The test-plane failure surface itself returns the largest ordered non-empty prefix that fits the agent envelope and always sets the last returned failure as the continuation cursor when either more store rows exist or the requested server page was shortened. Stable and advanced clients can therefore page through every record without gaps, duplicates, missing identities, or oversized replies. | Generate more than 64 distinct failures with maximum-sized diagnostics across multiple result chunks, ingest them, and page from both stable and advanced agent surfaces until exhaustion. Require one failure record per failed/error case, every reply at most 8192 bytes, exact monotonic cursors after size- and row-limited pages, no gaps/duplicates, and a final null cursor only after the complete retained index is reachable. Preserve one-record pages, process-only failures, malformed reporters, individual text bounds, maximum case/chunk/report limits, and normal small-run behavior. |
| UIL-TESTING-045 | Immutable live repository-state handles | A repository verifier read its authoritative uncommitted delivery-state SQLite database successfully in the primary checkout, but the immutable governed worker could see only snapshot content and standard dependency roots. Agents could neither run the required release verifier nor substitute a database copy without breaking the database identity and live-state contract. | A schema-3 test manifest may define a bounded named `sqlite` state handle and explicitly attach it to selected targets. The root snapshot/runtime boundary validates the exact contained real directory and database entry, pins their device/inode identities, binds that directory read-only at a fixed unit-private destination, and injects only the declared non-secret environment path. The database is never copied into snapshot or attempt storage, never made writable, and never exposed to targets that did not request the handle. Live replacement, path escape, symlink, unsafe entry, secret-shaped environment, unknown handle, duplicate destination, or identity drift fails before execution. | Define a repository-owned SQLite database with WAL sidecars, attach its handle to one immutable target, and prove the runner queries current live rows through the declared environment while a write fails. Prove another target cannot see it and no snapshot/artifact contains database bytes. Cover live and immutable plans, exact descriptor/catalog round-trip, root/entry replacement, symlink/path escape, unknown handle, duplicate environment/destination, malformed sidecars, changed database identity, same-schema service replacement, normal Python and `python -O`, and the installed DesignDocEngine delivery-state verifier against database identity `01bb41a2e9862c822299a587`. |
| UIL-TESTING-046 | Offline .NET restore argument classification | The immutable .NET bootstrap rejected the safe build-only `UseSharedCompilation` MSBuild property as an unknown restore graph selector, so a valid network-free test command failed before restore or test execution. | The runner distinguishes dependency-graph selectors from a bounded reviewed set of graph-neutral build properties. `UseSharedCompilation` accepts only an explicit boolean value, remains on the final `dotnet test --no-restore` command, and is omitted from the runner-owned locked restore because it cannot select packages. Unknown, malformed, source/cache, and graph-changing properties remain rejected. | Exercise `-p:`, `/p:`, and `--property:` spellings with true/false values and require the locked restore to omit the property while the final test command retains it. Reject missing, empty, non-boolean, similarly named, source/cache and unknown properties; preserve every existing configuration/framework/runtime graph selector and exact offline restore guard. |
| UIL-TESTING-047 | Verified artifact materialization | The agent artifact command exposed complete verified metadata and bounded text tails, but directory artifacts remained metadata-only. Release tooling therefore could not consume the retained archive bytes or bind its assembled receipt to the Coordinator-verified artifact without bypassing the public test interface. | The stable local test client exports any verified artifact through ordered bounded binary chunks from the exact run/artifact identity. Every chunk carries immutable combined identity, offset, total size and digest; the caller writes to a new file through a no-follow directory handle, verifies contiguous bytes and the full SHA-256, fsyncs, and publishes atomically without overwriting. No bytes enter the agent-result JSON, call journal, Console, or public network; export remains an explicit local command. | Export empty, one-chunk, multi-chunk, maximum-size, text, binary and directory artifacts. Require byte equality, exact full digest/size, stable identity across chunks, atomic mode-0600 publication and a bounded metadata-only result. Reject wrong run/artifact, unverified metadata, changed inode/size/content, gaps, overlaps, reordered/duplicate chunks, excessive chunks, symlink or non-directory parent, existing destination, short write, interrupted transfer and release replacement without leaving a published partial file. Reproduce the reported DesignDocEngine directory artifact through the installed client. |
