# Completion Ledger

- Re-run the original duplicate-project, unassigned-resource, background-refresh, repository-removal, and cropped-center-pane journeys through the clean signed packaged native app, and complete its package/launch-readiness gate.
- Complete the final freshness check and exact-commit remote matrix for the current cross-platform repair, including macOS Console E2E and Linux cross-UID CI.
- Bind normalized server and port lifecycle resolution to the exact local repository/host after an account database is restored on another host. The outer action guard and listener preflight are host-scoped, but downstream lifecycle services still resolve some canonical roots without `host_id`; verify public start, lease, assignment, and list actions against retained foreign same-root rows.
