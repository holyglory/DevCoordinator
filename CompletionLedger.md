# Completion Ledger

| ID | Remaining work | Why it matters | Verification |
| --- | --- | --- | --- |
| CL-2026-08-14-POSTGRES-VERIFY | Complete `bug-772adfcc05e94e0faf3a466e1cbae9d2` by strongly verifying and registering a current PostgreSQL backup. The stable broker command is deployed and produced a 6.42 GB custom-format dump, but the isolated scratch restore failed after 65 minutes. The host has 168 GB free while the attributed PostgreSQL volume footprint is about 512 GB; the existing safe cleanup plans reclaim only about 1.68 GB. Unblock by providing enough disposable verification capacity or a non-destructive capacity expansion. | A dump without a successful scratch restore is not proven recoverable and must not be presented as a usable strong backup. | Rerun the exact broker-owned backup command, require terminal success, retrieve the registered manifest through the installed client, and confirm `test_restore=true` against the immutable container identity. |
| CL-2026-08-14-ROUTE | Complete `bug-8a9d63b313e44de39586d4784b0c1d52` by publishing the exact `gf2` route. The host rejected the public-route mutation because changing that access boundary requires explicit authorization; no bypass or alternate mutation path is permitted. | The requested route remains login-protected and no publication receipt exists. | After the host authorization condition is satisfied, apply through the owner-controlled route surface, prove public `/` and `/api/health`, and verify the production Routes UI/API receipt. |
