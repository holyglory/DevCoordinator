# Completion Ledger

- Performance source and focused fixtures now split the host composition into disjoint project-runtime, Coordinator-control, Coordinator-background/scheduler, active-test and developer-session cgroup roots, with repository/browser/system/account detail kept non-additive. Before removal, complete software-owned deployment and live wide/narrow browser verification; confirm the installed topology actually places workloads in those roots, preserves a visible project-attribution gap when it does not, exposes measured process/shmem/kernel counters, and states that cgroup v2 does not provide PSS rather than inventing it.
- Live-verify the GlobalFinance admission corrections from UIL-TESTING-003 and UIL-TESTING-005. `manifest doctor` must not reject a protected immutable dependency because the calling Unix account cannot traverse it; the merged protected profile must route a duplicate repository through the calling UID's enrollment; and stable CLI `codex:` attribution must not enter the protected Google-delegation branch. Source has one exact actor, protected-profile, and repository-bound test-RPC contract, and the canonical Console evidence has been regenerated and deployed through a passing software-owned delivery cycle. Run GlobalFinance doctor and an installed protected immutable enqueue/follow canary, prove the submit reaches the test plane and returns a run handle with correlated bounded evidence, and remove this item.
- Deploy the verified authoritative project-metrics key contract. The source now records `repo:<repo-id>` and `family:<family-id>` histories alongside legacy path-key series, and the real-browser Projects popover test proves the root CPU/memory charts render. Production still serves the pre-fix release; switch it through the software-owned delivery workflow, wait for two 10-second samples, then open GlobalFinance’s Projects usage control and verify its CPU and Memory charts before removing this item.
- Recover the schema-12 bridge from the repository-repair crash loop before first adoption. The live bridge exited on 20 `disabled_repository_enabled_startup_policy` invariants for historical `/tmp` repository rows, and the earlier repair falsely reported restoration from transient systemd state without a stably ready broker socket. Source now provides the exact maintenance-locked policy compare-and-swap and dedicated restored-journal recovery with immutable evidence, strong socket/invariant proof, and owner/collaborator canaries. The retained forward-only journal is at the pre-export `predecessor-retired` boundary and already carries its sole client handoff to immutable release `64430`; that client binding and lineage must remain unchanged. Source now authorizes one distinct source-current outer executor through a singular sealed `executor_rescue` that binds the current inherited journal digests, immutable preimage, stopped live state, refreshed owner map, and executor identity, revalidates before export, and carries its digest through terminal completion evidence without creating another client handoff. Live cutover is not complete: preserve the contaminated releases and failure journal, stage the immutable rescue executor, invoke only the dedicated `successor-executor-rescue` route with the exact current pre-rescue raw/sealed digests and all three release identities, and prove owner export, candidate activation, authenticated canaries, terminal/completion evidence, and a stable clean bridge before removing this item.
- Live-verify the universal test harness from DC-2026-07-26-TEST-HARNESS-01. Source now provides schema-v12 repository-bound session/group/case journals, peer-authenticated start/finish/stat operations, a structured manifest runner, GlobalFinance thin declarations, arbitrary-window Console statistics, and bounded Board projections. The schema/profile cutover and authenticated server-wide inventory are live, and the Console projection passed deterministic long-content verification at 1440x900 and 390x844 with zero findings; this is not a substitute for executing the live journeys. Run one GlobalFinance individual selector, one group, and the full hermetic profile, then prove start/finish/case rows and 7/30/90-day authenticated Console views. On a macOS host, run the DevOps Board SwiftPM build/tests, launch the signed app, and prove the Tests tab at wide and narrow window sizes before removing this item.
- Activate and live-verify broker-owned ephemeral Docker admission from DC-2026-07-23-EPHEMERAL-01. Source now precommits exact repository/template ownership and an unguessable nonce, creates stopped, recovers only by all exact labels, persists cleanup intent, enforces quotas/TTL/safety, observes crashes, and excludes generic lifecycle controls. The schema/profile cutover is live, while repository templates still need enrollment and enrolled user sessions retain direct Docker-socket admission. Run the exact template/ACL consistency gate, enroll the repository templates, then remove direct Docker group/ACL access in a separate reversible administrator transaction after retained processes are drained. Live acceptance requires a normal run and a lost-reply recovery to appear under the exact repository, Finish/crash/expiry to remove the exact container and release its lease, and no agent-capable session to create a raw workload outside the broker. The existing unlabeled `gnt-artifact-pg` is not safe to auto-claim by name; use the fresh-inventory `resource attach` journey with its immutable resource, control-binding, immutable, and ownership fingerprints, then prove it leaves Unassigned Resources without changing the running container.
- Live-verify the 2026-07-18 exact access-request and Telegram notification journeys after their source and deterministic verification are complete. The broker/schema activation gate is live; remaining acceptance requires private Telegram bot registration through the live Console, an actual private `/start` approval, and observed delivery of one start, stop, failure, and crash event for an assigned project without leaking the bot token or notifying an unassigned/denied chat. Keep this item until the same public Console and Telegram surfaces prove the deployed behavior.
- Activate the completed typed cleanup lifecycle requested on 2026-07-18. Coordinator and Console source, exact live authorization, immutable plan/apply binding, reversible Archive/Restore, separately confirmed permanent removal, retained tombstones, no-resurrection and crash-replay controls, failure-shaped tests, and wide/narrow UI verification are complete. The target broker unit, generated home-access drop-in, schema 12 authority, and exact Console registration are live. The Console still defaults `LIFECYCLE_ENABLED` off, disables Archived views, and makes no lifecycle request until the whole capability is explicitly activated; enable the gate only after the database terminal-state and restore-replay blockers below are resolved, then exercise owner Archive, Restore, and Remove through the live Console before removing this item.
- Close the remaining broker-restart quiescence gap exposed during the 2026-07-18 `HOST_OBSERVE` rollout. The canonical broker unit/drop-in, API and Console soft-dependency, independent supervision, persistent journal, clean-exit, crash-restart, public HTTP, authenticated API, exact registration recovery, and clean broker restart are live-verified and no longer part of this item. Broker source fences mutation admission in the signal turn and drains admitted handlers for a configured graceful budget, but the configured wait is not proof that nested Docker/database work terminated. Resolve the database terminal-state and replay items below, then prove in-flight success/failure recovery before removing this item.
- Make PostgreSQL-in-Docker backup and restore timeouts terminal or explicitly outcome-uncertain. The broker times the helper process, but the helper launches nested `docker exec` and in-container PostgreSQL commands without a proven process-group/container-exec cancellation and terminal-identity check. A helper timeout can therefore return while `pg_dump` or `pg_restore --single-transaction` still runs. Add end-to-end bounded cancellation or durable outcome-uncertain reconciliation tied to the exact container/exec identity, and verify dump, verification, restore, timeout, disconnect, broker shutdown, and recovery cases before treating the graceful wait as quiescence.
- Make database restore-result registration idempotent across the registry-commit/operation-finish boundary. Restore metadata currently commits before the broker operation result; a disconnect or failure in that interval can replay registration, create another restore event, increment `restore_count` again, and return a different result for one physical restore. Bind the registry result and restore event to the broker operation ID (or an equivalent durable idempotency key), and add post-registry/pre-finish backup and restore replay controls before removing this item.
- Complete broker-owned unified runtime sessions and cross-UID lifecycle control. Source includes the enrolled ID-only `runtime.request` contract, freshly observed compact status, exact repository-family membership, family-wide unclassified-resource rejection, live per-action ACLs, and durable exact-ID start/stop/restart for enrolled Docker and database-stack targets. Docker aliases serialize on the same immutable container; every action requires fresh terminal proof, and ambiguous invocation, observation failure, or broker restart enters durable observation-only reconciliation without host re-execution. An ambiguous restart remains needs-attention when running state cannot prove its transition. Worker-role services now have peer-UID status/start/stop/restart, generation-checked replacement, staged permanent removal, native startup, and crash-loop supervision. Other service roles still return `runtime_supervisor_required`; shared TTL work returns `runtime_cleanup_owner_required`; and shared `run`, client-authored host details/ports, and Docker/database replacement remain rejected. Add the remaining service-role and target/lease supervision, created-versus-borrowed disposition for non-service targets, post-stop PID/listener/cwd proof, broker-owned TTL sessions/reaping, and service-owned artifacts. Then verify the remaining concurrent callers, forged/stale enrollment, PID reuse, wrong listener/cwd, disconnect, expiry, supersession, systemd restart, and original-owner recovery before describing all shared runtime combinations as ready.
- Implement safe account-authority `replace` for Docker and `database_stack` targets. Source now returns `unsupported_safe_replace` at request admission before opening the account store or resolving, observing, linking, or mutating a repository/resource; supplied Compose paths cannot bypass that boundary. Existing exact-ID start/stop/restart primitives cannot safely create a new immutable container identity, and account mode has no trusted stored Compose definition plus durable operation that atomically recreates and rebinds memberships, database bindings, routes, ports, cleanup ownership, rollback, and retry. Until that exists, runtime sessions can only borrow pre-enrolled Docker/database targets and intentionally retain their catalog rows during cleanup; add an exact created-versus-borrowed disposition and remove session-created containers/bindings from active inventory only after terminal identity proof. Database replacement is additionally blocked by the two terminal-timeout and restore-registration replay items above; it must create and strongly verify a fresh PostgreSQL backup bound to the old immutable container ID, restore and integrity-check the new immutable ID, and reconcile uncertain outcomes. Verify success, preflight rejection, identity drift, data preservation, backup/restore failure, crash at every commit boundary, retry/idempotency, supersession, and TTL/KillAfterRun cleanup before advertising either combination.
- Add service-owned Docker/database log capture to the shared broker runtime endpoint. Explicit account authority captures attention/failure logs through the unified runtime boundary using only the normalized target and exact 64-character container ID; publishes private immutable, secret-redacted, 1 MiB/2,000-line typed artifacts; retains exact-ID evidence after container removal; rejects identity replacement; and serves artifacts through the authenticated Console proxy without exposing host paths. The shared broker now owns exact-ID Docker/database lifecycle and inventory, but it has no narrow ACL-checked ID-only log-capture/read operation; do not let a client execute Docker or write a second artifact authority. Add broker-owned capture/read resolved from the service store, live repository grant checks, service-private artifacts and retention, then verify authorized/ungranted/revoked cross-UID reads, stopped/crashed/removed/replaced targets, broker restart, and Console delivery before removing this item.
- Confirm hover plus element selection on PRTZN through the authenticated Codex in-app browser. The user-approved split style-element policy is implemented in both canonical sources, all corrected guards and full suites pass, PRTZN is built and deployed through Coordinator, and its exact live response passes mobile/desktop parent-and-child viewport coverage while inline scripts remain blocked. Authenticated Console element-bound browser comments now prove that Console hover and selection work, so only PRTZN remains on the original acceptance boundary. Do not relax inline scripts, broad `style-src`, blob/data frames, or other directives without new violation evidence.
- Re-run the original duplicate-project, unassigned-resource, background-refresh, repository-removal, and worker Keep Alive/crash-loop/re-arm/removal journeys through the clean signed packaged native app.
