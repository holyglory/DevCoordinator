# Completion Ledger

- Activate and live-verify the universal test harness from DC-2026-07-26-TEST-HARNESS-01. Source now provides schema-v9 repository-bound session/group/case journals, peer-authenticated start/finish/stat operations, a structured manifest runner, GlobalFinance thin declarations, arbitrary-window Console statistics, and bounded Board projections. The Console projection has passed deterministic long-content verification at 1440x900 and 390x844 with zero findings; this is not a substitute for the live authenticated surface. Production still has the broker intentionally stopped on its pre-migration schema/profile, so no live run can be truthfully recorded until the existing controlled broker activation blockers clear. In the same rollback-safe activation, migrate the service database/profile, restart the broker and API, run one GlobalFinance individual selector, one group, and the full hermetic profile, then prove start/finish/case rows and 7/30/90-day authenticated Console views. On a macOS host, run the DevOps Board SwiftPM build/tests, launch the signed app, and prove the Tests tab at wide and narrow window sizes before removing this item.
- Activate and live-verify broker-owned ephemeral Docker admission from DC-2026-07-23-EPHEMERAL-01. Source now precommits exact repository/template ownership and an unguessable nonce, creates stopped, recovers only by all exact labels, persists cleanup intent, enforces quotas/TTL/safety, observes crashes, and excludes generic lifecycle controls. Production still has the broker intentionally stopped on its pre-migration schema/profile during the existing controlled maintenance window, and enrolled user sessions retain direct Docker-socket admission. Run the protected profile/database migration and exact template/ACL consistency gate, restart only through the existing rollback-safe cutover, enroll the repository templates, then remove direct Docker group/ACL access in a separate reversible administrator transaction after retained processes are drained. Live acceptance requires a normal run and a lost-reply recovery to appear under the exact repository, Finish/crash/expiry to remove the exact container and release its lease, and no agent-capable session to create a raw workload outside the broker. The existing unlabeled `gnt-artifact-pg` is not safe to auto-claim by name; use the fresh-inventory `resource attach` journey with its immutable resource, control-binding, immutable, and ownership fingerprints, then prove it leaves Unassigned Resources without changing the running container.
- Activate and live-verify the 2026-07-18 exact access-request and Telegram notification journeys after their source and deterministic verification are complete. Production activation still requires the controlled broker/schema activation gate blocked below, private Telegram bot registration through the live Console, an actual private `/start` approval, and observed delivery of one start, stop, failure, and crash event for an assigned project without leaking the bot token or notifying an unassigned/denied chat. Keep this item until the same public Console and Telegram surfaces prove the deployed behavior.
- Activate the completed typed cleanup lifecycle requested on 2026-07-18. Coordinator and Console source, exact live authorization, immutable plan/apply binding, reversible Archive/Restore, separately confirmed permanent removal, retained tombstones, no-resurrection and crash-replay controls, failure-shaped tests, and wide/narrow UI verification are complete. The Console now defaults `LIFECYCLE_ENABLED` off, disables Archived views, and makes no lifecycle request until the whole capability is explicitly activated; this removes the misleading partial-authority error without claiming cleanup is ready. Production still runs the old broker unit, generated home-access drop-in, and database schema. Apply the schema migration and generated drop-in only after the database terminal-state and restore-replay blockers below clear; then enable the gate, use the controlled broker restart path, prove exact coordinator/Console registration and health, and exercise owner Archive, Restore, and Remove through the live Console before removing this item.
- Close the remaining broker-restart quiescence gap exposed during the 2026-07-18 `HOST_OBSERVE` rollout. The API and Console soft-dependency, independent supervision, persistent journal, clean-exit, crash-restart, public HTTP, authenticated API, and exact registration recovery paths are now live-verified and no longer part of this item. Broker source fences mutation admission in the signal turn, drains admitted handlers for a configured graceful budget, and provides an exact transactional migration from the legacy Docker environment drop-in to the canonical broker unit. Production still runs the old broker unit/drop-in, and the configured wait is not proof that nested Docker/database work terminated. Apply that broker-only migration in a new private rollback transaction after the database terminal-state and replay items below are resolved; then prove in-flight success/failure recovery before removing this item.
- Activate the lifecycle-evidence server projection in the production broker at its next rollback-safe restart. Source and deterministic tests now keep enrollment-, ACL-, assignment-, and port-policy-only definitions in normalized `resources.servers` while excluding them from compatibility `servers`, project server counts, and lifecycle actions until a concrete lifecycle is observed; the Console has an equivalent rolling-upgrade filter so its live UI can be corrected without restarting the blocked broker. After the existing broker restart blockers clear, fresh-observe GlobalFinance and prove its seven `validation-port-lease` definitions remain available to the port workflow but absent from `v1_compatibility.servers` and `project_usage.server_ids`, while a real running and a real stopped server both remain visible and actionable.
- Make PostgreSQL-in-Docker backup and restore timeouts terminal or explicitly outcome-uncertain. The broker times the helper process, but the helper launches nested `docker exec` and in-container PostgreSQL commands without a proven process-group/container-exec cancellation and terminal-identity check. A helper timeout can therefore return while `pg_dump` or `pg_restore --single-transaction` still runs. Add end-to-end bounded cancellation or durable outcome-uncertain reconciliation tied to the exact container/exec identity, and verify dump, verification, restore, timeout, disconnect, broker shutdown, and recovery cases before treating the graceful wait as quiescence.
- Make database restore-result registration idempotent across the registry-commit/operation-finish boundary. Restore metadata currently commits before the broker operation result; a disconnect or failure in that interval can replay registration, create another restore event, increment `restore_count` again, and return a different result for one physical restore. Bind the registry result and restore event to the broker operation ID (or an equivalent durable idempotency key), and add post-registry/pre-finish backup and restore replay controls before removing this item.
- Complete broker-native cross-UID server lifecycle control. System-mode stop now checks the authenticated account's exact repository/server enrollment before reading a private journal, rejects identity drift before any reservation or signal, and returns a typed configuration/access failure for a host-visible but unenrolled target. An explicitly enrolled second UID still cannot stop or restart a server published by another UID because the broker has no typed server-stop operation and the caller cannot treat the original publisher's private journal as authority. The currently retained same-UID path still reserves from that client-owned journal and signals locally after only a static profile check, so it is not the final service-authoritative mutation boundary. Add live ACL reauthorization, service-store target and lease resolution, pidfd- or supervisor-bound control, post-stop identity/listener verification, authoritative lifecycle persistence, and private-journal reconciliation; verify authorized, ungranted, revoked, forged/stale journal, PID-reuse, wrong-listener, wrong-cwd, systemd, and original-owner recovery cases before removing this item.
- Confirm hover plus element selection on PRTZN through the authenticated Codex in-app browser. The user-approved split style-element policy is implemented in both canonical sources, all corrected guards and full suites pass, PRTZN is built and deployed through Coordinator, and its exact live response passes mobile/desktop parent-and-child viewport coverage while inline scripts remain blocked. Authenticated Console element-bound browser comments now prove that Console hover and selection work, so only PRTZN remains on the original acceptance boundary. Do not relax inline scripts, broad `style-src`, blob/data frames, or other directives without new violation evidence.
- Re-run the original duplicate-project, unassigned-resource, background-refresh, repository-removal, and cropped-center-pane journeys through the clean signed packaged native app, and complete its package/launch-readiness gate.
- Bind normalized server and port lifecycle resolution to the exact local repository/host after an account database is restored on another host. The outer action guard and listener preflight are host-scoped, but downstream lifecycle services still resolve some canonical roots without `host_id`; verify public start, lease, assignment, and list actions against retained foreign same-root rows.
