# Completion Ledger

| Item | What remains | Why it matters | Verification |
| --- | --- | --- | --- |
| Disposable Test Store cutover | Remove legacy history migration/admission paths, add the fresh incompatible-store cutover, and delete compatibility-only target CPU/memory/PID schema. | Retaining disposable compatibility state is a major source of code and contract drift. | Packaging and static checks find no retired imports/fields; fresh-store and incompatible-cutover tests pass. |
| Single lifecycle authority | Persist the complete plan execution specification and make testd own deadline, result order, terminal conclusion, and collection decisions while the privileged runtime remains an idempotent host executor. | Submission-critical in-memory state and competing lifecycle interpretation caused stuck, regressed, and misclassified runs. | Replacement, deadline, cancellation, result-stream, lease, and exact-worktree tests pass without duplicate launch or terminal regression. |
| Driver boundaries | Extract framework-specific dependency, invocation, and reporter behavior behind normalized driver interfaces. | Python, Node, .NET, and reporter details currently expand the scheduler/runtime core and multiply contract combinations. | Existing driver/reporter/dependency regression cases pass through the new interfaces; core modules contain no framework-specific branches. |
| Routine agent API | Remove redundant stable status/summary/wait aliases, keep enqueue/submit/follow/cancel primary, and preserve bounded diagnostic continuations and advanced drill-down. | Multiple routine projections drifted and returned contradictory envelopes. | CLI, MCP, capability, documentation, result-bound, and installed-client tests agree on one routine surface. |
| Pre-split authority path binding | Preserve the original integrity-gated normalization/write for already sealed pre-split cutover ledgers, or retire that compatibility only after explicit user approval. The simplification checkpoint currently rejects the legacy sealed shape, and its focused regression fails. | This is a security-relevant recovery control, not disposable test history; changing it silently could strand or misbind an existing authority cutover. | `test_pre_split_ledger_binds_distinct_final_authority_once` passes with exact source/final paths, replay, seal, and state-generation evidence, or a user-approved superseding decision removes the path and its acceptance case. |
| Delivery | Run the repository-owned complete validation/delivery cycle and reconcile every failure before readiness. | This cross-cutting change is not complete with only focused source checks, and DevCoordinator must not depend on its installed test/runtime surfaces while under repair. | `software_owned_delivery.py` completes with repository, package, installed-client, governed-test, and browser evidence; this ledger is then removed. |
